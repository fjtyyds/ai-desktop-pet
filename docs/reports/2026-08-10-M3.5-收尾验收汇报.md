# M3.5 收尾验收汇报（2026-08-10）

## 一、验收与合并结果

T-24~T-31 共 8 个任务全部完成并通过验收，已按序合并到 main：

| 任务 | 内容 | 合并提交 |
| --- | --- | --- |
| T-27 | 番茄钟重复通知修复 | 34a2679 |
| T-24 | 窗口布局与可缩放 | 39aa346 |
| T-25 | 工具栏（导出 + 最小化） | 01e35a8 |
| T-26 | 天气自动刷新增强 | 827abb6 |
| T-28 | 人格模板文案精简 | 0415d20 |
| T-29 | 全局快捷键移除 | 875150e |
| T-30 | 系统状态小部件移除 | 4bbf6cb |
| T-31 | 贴边方案 B（靠边吸附不自动隐藏） | b8cd946 |

## 二、验证情况

- 8 个分支独立 `npm run check`：全部通过。
- 合并过程中共享文件冲突（check.js、chat.js、main.js、preload.js、chat.css、store.js 等）已逐一手工解决；合并后 main `npm run check` 全部通过。
- `npm run smoke`：通过（真实用户权限；含番茄钟通知信号幂等、渲染层→主进程一次性通知断言）。
- UI 目检：各线程已完成无头/DOM 级验证，最终视觉效果建议用户以 `npm run dev` 人工复核（窗口缩放、工具栏、天气、贴边吸附、TTS 等）。

## 三、重复 T-27 说明

用户反馈多开了一个 T-27 线程。核对 reflog 与分支后确认：`codex/m3x-pomodoro-fix` 仅有一次实现提交（d0d4595），未产生第二个分支或重复实现；保留现有实现并合并。

## 四、当前项目进展

- M0~M3 完成；M3.5 十项功能（T-14~T-23）完成。
- M3.5 收尾优化（T-24~T-31）完成并合并：窗口可缩放、小部件不再遮挡聊天、底部平台/版本信息移除、导出移入工具栏、新增最小化、天气 15 分钟自动刷新与失败重试、番茄钟弹窗修复、人格模板精简、全局快捷键移除、系统状态小部件移除、贴边改为靠边吸附。
- 新增需求：TTS 专属语音包（ADR-027 / T-33）。

## 五、下一步计划

1. 整体 UI 人工目检（`npm run dev`），确认上述改动在真实桌面环境无回归。
2. T-32：orchestrator 缺陷修复（多 --task 参数、token/时间预算、失败转人工；是否入库待用户决定）。
3. T-33：TTS 专属语音包（按人格配置 voice/pitch/rate）。
4. Backlog：UI 大改（M3.6 独立里程碑）。
5. M4：GitHub 开源 + Actions 发布 + Microsoft Store/Steam；代码签名、自动更新评估；用最终 main 重新 `npm run dist`。

## 六、遗留事项

- “多线程开发/”（约 5GB）按用户指示暂时保留，后续可清理。
- `scripts/orchestrator/` 未入库，去留待用户决定。
- 旧 M3.5 worktree（m3x-export/idle/memory-ui/mood-ui/stream/widgets）仍存在，是否清理待用户决定。
- 安装包仍是 M3 版本，M4 前需用最终 main 重新打包。
