# 新会话交接（2026-08-10）

> 给下一个 Codex 会话的冷启动材料。先读本文件，再读 AGENTS.md、PLAN.md、docs/STATUS.md、docs/SPEC.md、docs/DECISIONS.md、docs/API.md。

## 一、当前状态快照

- main 最新提交：`311bad5`（docs: M3.5 全自动开发工作汇报）；工作区干净（唯一未跟踪：scripts/orchestrator/）。
- M0~M3.5 全部完成：T-01~T-23 已合并，`npm run check` / `npm run smoke` 通过。
- M3.5 十个功能：流式回复、空闲互动、情绪可视化、记忆管理、导出/清除、贴边/快捷键、首次引导+人格模板、系统状态+番茄钟、天气、TTS（STT/图片验证不可行已记录降级）。
- 自动开发工具：scripts/orchestrator（未入库）已实战打通（T-19~T-23）；codex CLI 为 npm 版 0.147.0。

## 二、待解决问题清单

### P0（目检未闭环）

1. M3.5 整体人工目检尚未收到最终结论：首次引导、人格模板、系统状态/番茄钟、天气、TTS、以及全部旧功能回归。若用户报 bug，按“一个会话一个任务”修复。

### P1（待用户决策）

2. 根目录未跟踪“多线程开发/”（约 5GB，12 个 worktree 副本 + node_modules，已加入 .gitignore）：保留还是删除，等用户决定；不要擅自删。
3. scripts/orchestrator/ 是否入库：建议入库并先修复已知缺陷（见 P2-4），入库前经用户确认。

### P2（工程改进）

4. orchestrator 已知缺陷：多 `--task` 参数只保留最后一个（当前用“一任务一进程”规避）；建议修复参数解析并补 selftest。
5. orchestrator 增加 token/时间预算：记录每次 codex exec 的耗时与输出量，支持按任务限额、2 次失败即转人工。
6. 空闲互动时长不透明（用户反馈等待时间长）：设置页加“空闲触发时长”选项并显示当前值（AI_PET_IDLE_TRIGGER_MS 已存在，需 UI 化）。
7. 文档更新时间戳：PLAN.md / STATUS.md 仍写 2026-08-09，可顺手改为 2026-08-10。

### P3（后续里程碑）

8. M4 分发规划：GitHub 开源 + Actions 发布 + Microsoft Store/Steam；代码签名、自动更新评估；用最终 main 重新 `npm run dist`（现有安装包是 M3 版本）。
9. T-23 降级项复查：语音输入（STT）与图片理解验证不可行已记录，后续如需可引入本地 STT 或视觉模型再评估。
10. CI 未在 GitHub Actions 实际跑过 M3.5 后的版本（本地 check/smoke 通过），开源前验证 CI。

### 环境注意事项（新会话必读）

- PowerShell 执行策略禁止 npm.ps1：用 `npm.cmd`。
- smoke 需真实用户权限（沙箱内 Electron GPU 崩溃）。
- git 写操作（合并/提交/worktree）需提权。
- 若应用“启动即退出”：先查托盘/进程/`%APPDATA%\ai-desktop-pet\lockfile`（旧实例占用单实例锁），taskkill /IM electron.exe /F 后重启。
- DeepSeek Key 已在对话中多次出现，建议用户轮换；settings.json 中 apiKey 已加密（T-13）。

## 三、新会话首次提示词（可直接粘贴）

```text
你是 AI 桌宠项目的协调者/开发助手。开始前请完整阅读并按顺序消化：
1. AGENTS.md（项目规则）
2. docs/STATUS.md 与 PLAN.md（当前状态与计划）
3. docs/SPEC.md 与 docs/DECISIONS.md（需求与决策）
4. docs/API.md（接口约定）
5. docs/reports/2026-08-10-全自动开发工作汇报.md 与 docs/reports/2026-08-10-新会话交接.md（本会话交接）

环境要点：PowerShell 下用 npm.cmd；npm run check 每次改动后必须通过；npm run smoke 需要真实用户权限；git 写操作需提权；涉及 UI 用 npm run dev 目检；一个会话只做一个任务，先写 ADR/更新任务卡再实施。

请先只做一件事：向我确认你对项目状态的总结（main 当前提交、已完成里程碑、未解决问题清单），并给出下一步建议顺序，不要直接开始改代码。等我确认后再开始第一个任务。
```
