# M4 分发规划（2026-08-10）

> 目标：把 AI桌宠 分发到 GitHub 开源 + Microsoft Store + Steam，并补齐 CI/CD、签名、自动更新评估与最终打包。本文件为规划，不产生外部副作用；每个阶段的实际执行（建仓、注册、发布、付费）均需用户确认。

## 一、现状

- main：bee63bb（T-34/T-35/T-36 已合并；TTS v2 与贴边待用户复核）
- 安装包：仍是 M3 版本，M4 前需用最终 main 重新 `npm run dist`
- 已定：`scripts/orchestrator/` 不入库（ADR-028）；专属语音克隆暂缓（ADR-030）

## 二、阶段规划

### P0 开源前仓库整理（无外部副作用，可先行）

- 处理未跟踪大目录“多线程开发/”（约 5GB）：是否清理待用户决定；开源前必须从任何提交/发布物中排除
- 完善 `.gitignore`：dist/、logs/、userData、本地探针文件（probe-*.js、test-edge-tts.js、restart-dev.ps1、v2-*.mp3 等）
- 补充开源文件：LICENSE（建议 MIT，待用户确认）、README（安装/使用/自带密钥说明）、贡献指南、安全策略
- 核对无凭证入库（apiKey 均走 safeStorage；检查历史提交无密钥）

### P1 CI/CD

- GitHub Actions：windows-latest 上 `npm ci` → `npm run check` → `npm run smoke`（需解决无头 GPU/AppData 问题）→ `npm run dist` → 产物上传 artifact / release 草稿
- 需要 GitHub 账号与仓库名（用户提供/确认）

### P2 签名与商店

- 代码签名：评估 EV/OV 证书成本与 SmartScreen 影响；免费自签仅适合内部使用
- Microsoft Store：开发者账户注册（一次性费用）、MSIX 打包、审核流程
- Steam：Steamworks 账户（约 $100 一次性）、Steam Pipe Tools、商店页与审核

### P3 自动更新评估

- GitHub Releases + electron-updater（推荐评估）；商店渠道以商店更新机制为准
- 结论写入 ADR 后实施

### P4 最终打包

- 用最终 main `npm run dist`，安装/卸载/启动冒烟

## 三、待用户决策（按时间顺序）

1. 是否先做 M3.6 UI 大改评估，还是直接进入 M4？（建议：UI 大改可后置，M4 先行开源与 CI）
2. “多线程开发/”（5GB）与旧 M3.5 worktree（6 个）是否清理
3. GitHub 仓库名与账号；是否允许创建公开仓库并推送
4. 代码签名证书预算与商店账户注册意愿
5. TTS v2 听感与贴边真人拖动复核结论（如未复核，不影响 M4 规划推进）

## 四、风险

- SmartScreen 未签名警告；商店审核时长；Steam 上架门槛与审核
- 在线服务（DeepSeek/Edge TTS/天气）需要用户自带密钥或网络的合规说明
- Electron 包体 ~100MB，商店体积限制与下载体验
