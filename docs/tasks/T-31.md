# T-31 贴边体验优化

- 状态：已完成（2026-08-10 合并 main @ b8cd946）
- 分支：codex/m3x-dock
- 工作树：E:\codex\AI桌宠-m3x-dock
- 目标：窗口贴边体验差，按确认后的方案优化
- 涉及文件：
  - src/main/main.js（DOCK_* 常量、dockWindow/pollDock/undockWindow 等贴边逻辑）
  - src/renderer/chat.js（贴边开关文案与交互）
  - src/renderer/index.html（如设置项调整）
  - src/storage/store.js（dockEnabled 语义调整）
  - src/shared/locales/zh-CN.json、en.json（文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：已完成（ADR-026 确认方案 B）
- 验收标准：
  1. 按确认方案行为稳定：拖到边缘触发正确，无意外隐藏/卡死/反复滑出
  2. 拖动离开边缘可正常恢复，位置记忆仍生效
  3. 与最小化、隐藏到托盘、缩放窗口不冲突
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。
- 协调者合并：b8cd946（2026-08-10；check/smoke 通过）

## 完成记录（2026-08-10，T-31 开发线程）

- 分支/提交：codex/m3x-dock @ 56fb2b9
- 方案：ADR-026 方案 B——靠边吸附、不自动隐藏；保留 `window:toggle-dock` 契约与 `dockEnabled` 设置项，语义调整为“吸附开关”。
- 改动清单：
  - `src/main/main.js`：删除 DOCK_STRIP/HOLD/POLL/SLIDE/LINGER 等自动隐藏参数与 pollDock/slideWindowTo/computeHiddenBounds/动画/轮询整套逻辑；`findDockEdge` 改为取最近边缘；拖放结束（moved）触发吸附；拖动离开边缘立即取消吸附；新增 resize 处理保持吸附边贴齐；吸附/取消吸附均持久化位置记忆。
  - `src/renderer/chat.js`：贴边开关文案改为“靠边吸附/Edge snapping”，提示改为“不自动隐藏、拖动离开边缘恢复”；控制台提示同步。
  - `src/storage/store.js`：`dockEnabled` 注释语义更新为靠边吸附开关（字段与清洗逻辑不变）。
  - `scripts/check.js`：新增 T-31 同步断言（main.js 无自动隐藏逻辑、chat.js 文案同步、store dockEnabled 读写清洗）。
  - `src/shared/locales/zh-CN.json`、`en.json`：未改动——贴边文案在 chat.js 双语内联（沿用 T-19 结构），locale 文件无相关键。
- 自检结果：`npm run check` 通过；`npm run smoke` 通过（需真实用户权限，沙箱内 GPU 崩溃为已知问题）。
- 目检结论：待人工目检——建议拖动到四边验证吸附/离开恢复、设置页开关、最小化/隐藏到托盘/缩放回归。
- 边界外触碰：无；未改 package.json、contracts.js、docs/API.md、PLAN/STATUS/AGENTS/DECISIONS、scripts/orchestrator。
