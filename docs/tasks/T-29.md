# T-29 全局快捷键移除

- 状态：已完成（2026-08-10 合并 main @ 875150e）
- 分支：codex/m3x-shortcut-remove
- 工作树：E:\codex\AI桌宠-m3x-shortcut-remove
- 目标：全局快捷键功能冗余，移除快捷键注册、设置项与相关契约
- 涉及文件：
  - src/main/main.js（globalShortcut 导入、SHORTCUT_CANDIDATES、tryRegisterShortcut/unregisterShortcut、handleSetShortcutEnabled）
  - src/main/preload.js（移除 petAPI.window.setShortcutEnabled）
  - src/storage/store.js（移除 shortcutEnabled 默认值/清洗/白名单）
  - src/renderer/chat.js（移除快捷键开关与文案）
  - src/renderer/index.html（移除快捷键设置项）
  - src/shared/locales/zh-CN.json、en.json（移除快捷键文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md（协调者按 ADR 移除契约前保持只读；移除后按新签名实现）
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：已完成（ADR-026 移除契约，contracts.js/API.md 已同步）
- 验收标准：
  1. 应用不再注册任何全局快捷键
  2. 设置页无“全局快捷键”开关与提示
  3. 旧 settings.shortcutEnabled 字段被忽略或兼容清理，不报错
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。
- 协调者合并：875150e（2026-08-10；check/smoke 通过）

## 完成记录（2026-08-10）

- 实现：按 ADR-026 移除全局快捷键——`src/main/main.js` 删除 globalShortcut 导入、SHORTCUT_CANDIDATES、tryRegisterShortcut/unregisterShortcut、handleSetShortcutEnabled、启动注册与 will-quit 清理；`src/main/preload.js` 移除 window:set-shortcut 通道与 petAPI.window.setShortcutEnabled；`src/storage/store.js` 移除 shortcutEnabled 默认值/清洗/白名单，并在 readSettings 时兼容清理旧字段（旧 settings.json 字段不再报错、不再暴露）；`src/renderer/chat.js` 移除快捷键开关与双语提示（保留贴边开关与 toggleDock）；`scripts/check.js` 新增 T-29 断言。
- 未改动：`src/renderer/index.html` 与 `src/shared/locales/zh-CN.json`、`en.json` 中无快捷键静态项（设置项原由 chat.js 动态注入），故无内容可移除；`src/shared/contracts.js`、`docs/API.md` 按约定只读未触碰。
- 校验：`node --check` 五个涉及 JS 文件通过；`npm run check` 全部通过（含新增 T-29 断言）；`npm run smoke` 通过（需真实用户权限，沙箱内 GPU 崩溃为项目已知现象）。
- UI 验证：临时 Electron 脚本（offscreen）加载真实渲染页断言——设置页无 `shortcut-enabled` 开关、petAPI.window.setShortcutEnabled 为 undefined；贴边开关与 toggleDock 回归正常；预置旧 settings.shortcutEnabled=true 启动无报错。未运行 `npm run dev` 人工目检（自动化线程无法人工操作窗口），标记“待人工目检”；截图保存于 Codex 可视化目录（T29-settings.png）供协调者复核。
