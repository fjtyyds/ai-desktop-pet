# T-29 全局快捷键移除

- 状态：未开始（待用户确认后启动；前置：协调者移除契约 window.setShortcutEnabled）
- 分支：codex/m3x-shortcut-remove
- 工作树：E:\codex\AI桌宠-m3x-shortcut-remove
- 目标：全局快捷键功能冗余，移除快捷键注册、设置项与相关契约
- 涉及文件：
  - src/main/main.js（globalShortcut 导入、SHORTCUT_CANDIDATES、tryRegisterShortcut/unregisterShortcut、handleSetShortcutEnabled）
  - src/main/preload.js（移除 petAPI.window.setShortcutEnabled）
  - src/storage/store.js（移除 shortcutEnabled 默认值/清洗/白名单）
  - src/renderer/chat.js（移除快捷键开关与文案）
  - src/renderer/index.html（移除快捷键设置项）
  - src/shared/locales/zh-CN.json、en.json（移除快捷键文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md（协调者按 ADR 移除契约前保持只读；移除后按新签名实现）
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：协调者先完成契约移除（contracts.js、API.md、ADR 同步修订）
- 验收标准：
  1. 应用不再注册任何全局快捷键
  2. 设置页无“全局快捷键”开关与提示
  3. 旧 settings.shortcutEnabled 字段被忽略或兼容清理，不报错
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。
