# T-40 许可证与付费墙（买断/订阅/功能门控）

- 状态：已完成（待协调者验收）（2026-08-11 更新）
- 分支：codex/m4-license（建议）
- 工作树：E:\codex\AI桌宠-m4-license（建议）
- 目标：建立本地许可证体系：免费/Pro 订阅/永久买断三种状态，设备绑定激活码或订单号，功能按版本门控（云额度、高级语音、皮肤市场、专注统计等），并加入内容合规声明（年龄确认/人格白名单）。
- 涉及文件：
  - src/main/license.js（新增：许可证状态机、激活/校验/过期处理、设备绑定）
  - src/main/ipc.js、src/main/preload.js（license:get/activate/deactivate 通道）
  - src/renderer/chat.js、index.html、chat.css（设置页“账户/订阅”区块与门控 UI）
  - src/storage/store.js（license 相关设置白名单：licenseTier/licenseKey/deviceId）
  - src/shared/locales/zh-CN.json、en.json（订阅文案）
  - scripts/check.js（断言：许可证状态机、门控接线、双语文案）
  - docs/tasks/T-40.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md、docs/reports/2026-08-11-商业化上线方案.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、docs/**（除本卡外）
- 依赖：无；支付接入为 T-41（本卡先做离线激活码/订单号校验接口，支付回调留桩）
- 验收标准：
  1. 免费/Pro/永久三态可切换并持久化；功能门控生效（如免费版云额度受限、高级语音锁定）
  2. 激活码/订单号校验接口可 mock 验证（不接真实支付），过期/吊销可处理
  3. 首次启动年龄确认与内容合规声明弹窗
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
  5. 不触碰 package.json/contracts/API.md
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push；回报协调者。

## 完成记录（2026-08-11）

- 分支：codex/m4-license（独立 worktree：E:\codex\AI桌宠-m4-license）
- 提交：见 git log；未合并 main、未 push
- 实现摘要：
  - src/main/license.js（新增）：free/yearly/lifetime 三态状态机；激活码/订单号
    mock 校验（PRO-YEARLY-/PRO-LIFETIME-/ORDER-）；设备绑定（主机名+用户名+平台
    哈希派生 deviceId）；过期/吊销/设备不一致处理（降级 free）；云 AI 额度
    （free 10/日、yearly 200/月、lifetime 按免费档 10/日）；支付回调桩
    handlePaymentCallback（T-41 接入真实支付）
  - IPC/preload：license:get / license:activate / license:deactivate；
    chat 发送在主进程做云额度二次校验（BYOK 不消耗）
  - 设置页“账户/订阅”区块：档位/状态/有效期/额度/激活/注销；门控：
    免费版锁定高级神经语音（TTS 开关禁用）、隐藏皮肤市场/专注统计/待办入口，
    Pro/买断解锁；免费版保留 BYOK 对话、本地记忆、番茄钟、天气
  - 首次启动年龄确认+内容合规声明弹窗：未同意前禁用聊天输入，同意后
    complianceAccepted=true 持久化、不再弹；拒绝后 AI 对话停用（本地功能保留）
  - store.js 白名单新增 licenseTier/licenseKey/licenseExpiresAt/deviceId/
    complianceAccepted（默认 free/''/0/''/false，含清洗规则）
  - locales zh-CN/en 增加 license.* 订阅与合规文案
  - scripts/check.js 追加 T-40 断言（三态/门控/接线/白名单/双语文案/状态机）
- 验证结果：
  1. `npm run check` 通过（末行 `[check] 全部通过`；T-40 断言全 OK）
  2. `npm run smoke` 通过（渲染加载/历史恢复/番茄钟信号/chat 表单端到端，气泡 4）
  3. 三态切换与门控：临时端到端 UI 探针（Electron offscreen）验证
     免费→Pro→免费切换、额度 200/200 展示、高级语音解锁/锁定、皮肤入口
     显示/隐藏、注销按钮显示/消失，全部符合预期（探针为临时文件，已删除）
  4. 合规弹窗：首次启动可见且聊天输入禁用 → 同意后隐藏、输入恢复、
     complianceAccepted 持久化；拒绝路径由代码保证 AI 对话停用
  5. 设备绑定/吊销/过期：check.js 断言通过（device-mismatch 降级 free、
     吊销码拒绝、订阅过期降级 free）
- 边界外触碰：无（package.json/package-lock/electron-builder.yml/.github/
  scripts/orchestrator/contracts/API.md/PLAN/STATUS/DECISIONS/AGENTS 均未改动）
- 遗留风险：
  - 真实支付/订单服务端校验由 T-41 接入；当前仅本地 mock
  - 云额度按“未填 BYOK Key 的每次 AI 调用”计次（当前无 Key 时走 demo/mock），
    T-41 接真实云通道后语义不变
  - lifetime 不含云额度，当前按免费档 10/日执行；如需“0 云额度”可在
    license.js CLOUD_QUOTA.lifetime 调整
  - 涉及 UI 的改动建议协调者用 `npm run dev` 人工目检一次（本线程为无头验证）

## 协调者验收（2026-08-11）

- 6ac339b 经 merge 合并入 main（dece63a，共享文件冲突由协调者解决）；main check/smoke 通过；worktree 已清理，分支保留。
