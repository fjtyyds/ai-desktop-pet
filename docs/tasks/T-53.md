# T-53 修复 stopDockPolling 未定义（最小化窗口报错）

- 状态：已完成（已验收合并，2026-08-11）
- 分支：codex/m4-dock-fix
- 工作树：E:\codex\AI桌宠-m4-dock-fix（协调者已创建，基线 main 16dc4cd）
- 目标：修复 `src/main/main.js` 最小化事件处理器调用未定义的 `stopDockPolling()` 导致 ReferenceError 的回归 bug。
- 背景：T-25 设计为“最小化时停止贴边轮询”；T-31/T-35 将实现改为 move 防抖（`dockMoveDebounceTimer` + `scheduleDockMoveEnd`）后不再有轮询，`stopDockPolling()` 调用残留且全仓无定义，最小化窗口时日志反复报 `stopDockPolling is not defined`（由其他项目线程实测发现，协调者已核实 main.js:497 唯一引用、无任何定义）。
- 涉及文件：
  - src/main/main.js
  - scripts/check.js
  - docs/tasks/T-53.md（本卡）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/shared/contracts.js、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡外）
- 修复要求：
  1. 消除未定义引用。二选一（任选，必须说明选择）：
     a. 在 main.js 定义 `function stopDockPolling() { clearTimeout(dockMoveDebounceTimer); }`（保持 minimize 调用与 T-25 语义，注释说明 T-31 后已无轮询、仅清理防抖定时器）；
     b. 删除 `stopDockPolling()` 调用，minimize 处理器直接 `clearTimeout(dockMoveDebounceTimer)`。
  2. 核对 `dockMoveDebounceTimer` 声明与清理时序（与 window closed/quit 清理不冲突）；不得改变吸附/防抖/位置保存行为。
  3. check.js 增加防回归断言：源码中出现 `stopDockPolling(` 调用时，必须存在其函数声明/定义（如 `function stopDockPolling` 或 `const stopDockPolling`），否则 fail。
  4. 验证：`npm run check`、`npm run smoke` 全绿；如可行，用 stub/静态方式验证 minimize 路径不再抛 ReferenceError（不得引入真实窗口副作用）。
- 验收标准：
  1. `npm run check` 全绿（含新增防回归断言）
  2. `npm run smoke` 全绿
  3. 全仓无未定义的 stopDockPolling 引用；贴边/防抖/位置保存逻辑无行为变化
  4. 未触碰禁止文件；无外部副作用；不合并 main、不 push
- 完成定义：实现并自检（check/smoke），更新本卡状态为“已完成（待协调者验收）”，提交分支；回报（分支/提交/改动文件清单/修复方式选择/验证结果/残留说明）。
- 备注：worktree 已由协调者建好且 node_modules 已装；不要新建其他 worktree，不要改 main 工作区。

## 完成记录（worker 填写）

- 状态：已完成（待协调者验收）
- 分支：codex/m4-dock-fix
- 修复方式选择：方案 a（定义 `function stopDockPolling() { clearTimeout(dockMoveDebounceTimer); }`）
- 实现摘要：
  - main.js：保留最小化处理器中的 `stopDockPolling()` 调用（T-25 语义），在 `scheduleDockMoveEnd` 之后新增同名函数定义，仅清理 `dockMoveDebounceTimer`；注释说明 T-31/T-35 起已无轮询、该调用只清理 move 防抖定时器，避免对最小化窗口执行拖放结束处理。
  - 时序核对：`dockMoveDebounceTimer` 在模块顶层声明（null 初值）；`scheduleDockMoveEnd` 每次先 clearTimeout 再重建，回调执行后置 null；`closed` 事件处理器在销毁时 clearTimeout 并置 null。新增函数只做幂等 clearTimeout，与 closed/quit 清理不冲突；未改动吸附、防抖、位置保存的任何逻辑。
  - scripts/check.js：新增 T-53 防回归断言——递归扫描 `src/**/*.js`，凡出现 `stopDockPolling(` 调用（扣除函数声明本身）必须存在 `function stopDockPolling` 或 `const/let/var stopDockPolling` 定义，否则 fail。
- 改动文件：
  - src/main/main.js
  - scripts/check.js
  - docs/tasks/T-53.md（本卡）
- 验证结果：
  - `npm run check` 全绿（含新增断言：`[check] OK: T-53 stopDockPolling 调用（1 处）均存在定义`，结尾 `[check] 全部通过`）
  - `npm run smoke` 全绿（渲染页加载成功、启动历史恢复通过、chat 表单端到端通过）
  - stub/静态验证：提取 main.js 中 `stopDockPolling` 定义与 minimize 处理器回调，在 Node 沙箱中以假 `clearTimeout`/`dockMoveDebounceTimer=12345` 执行，无 ReferenceError，且 clearTimeout 确实收到该定时器（PASS）
  - 全仓检索：`stopDockPolling` 唯一代码引用在 src/main/main.js（定义 + 1 处调用），其余命中仅为文档
- 遗留风险：
  - 最小化瞬间若防抖定时器已触发（回调已从队列取出执行），clearTimeout 为无效操作，与修复前预期行为一致，无新增风险
  - 真实窗口最小化路径未经 Electron 实机触发（本卡约束不得引入真实窗口副作用），由协调者合并后可按需 `npm run dev` 目检

## 验收合并记录（协调者填写）

- 复核：改动 3 个文件均在任务卡允许范围（main.js/check.js/T-53.md）；方案 a（定义 stopDockPolling 清理 dockMoveDebounceTimer）保留 T-25 语义且断言可校验；分支基于 23a6e3d，fast-forward 合并 70a5b75。
- 验证：worker 与协调者 `npm run check`、`npm run smoke` 全绿；T-53 防回归断言（调用必须有定义）通过；吸附/防抖/位置保存逻辑无改动。
- 遗留：无（最小化路径 ReferenceError 已消除；真机最小化行为可随 dev 目检复核）。
