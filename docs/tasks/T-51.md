# T-51 整体移除专注统计组件（番茄钟数据源已删）

- 状态：已完成（已验收合并，2026-08-11）
- 分支：codex/m4-focus-stats
- 工作树：E:\codex\AI桌宠-m4-focus-stats（协调者已创建，基线 main 41cc892）
- 目标：专注统计（focusStats/focus-widget）唯一数据源（番茄钟完成）已于 T-50 移除，组件永不增长；本次全链路移除，避免误导用户。喝水提醒与待办组件保留（数据源独立）。
- 涉及文件：
  - src/renderer/index.html
  - src/renderer/chat.js
  - src/renderer/chat.css
  - src/storage/store.js
  - src/main/license.js
  - src/shared/locales/zh-CN.json、en.json
  - scripts/check.js
  - scripts/smoke.js
  - docs/tasks/T-51.md（本卡）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/shared/contracts.js、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡外）
- 专注统计移除要求（全链路清理）：
  - index.html：删除 #focus-widget 区块（专注统计卡片）；账户/订阅 Pro 功能列表中的“专注统计”条目（license.featureFocusStats）一并删除；喝水/待办组件保留。
  - chat.js：删除 focusWidget 元素引用、normalizeFocusStats/renderFocusStats/recordFocusSession（含 addFocusStats 相关）等 focus 专属函数与导出（getFocusStats）、设置读写与保存分支、相关注释中的“专注统计”字样；待办/喝水逻辑与导出保留。
  - chat.css：删除 #focus-widget/.focus-* 样式（含 focus 专属变量）；喝水/待办样式保留。
  - store.js：DEFAULT_SETTINGS 移除 focusStats；删除 sanitizeFocusStats 与 merge/whitelist/patch 中 focusStats 分支；存量 settings.json 的 focusStats 字段在读写时删除（不迁移、不兼容读取，同 ADR-038 番茄钟处理）。
  - license.js：entitlements 移除 focusStats（todos 保留）；相关注释同步。
  - locales（zh-CN/en）：删除 focus.*（title/proOnly/widgetAriaLabel 等）与 featureFocusStats；待办/喝水文案保留。
  - check.js：删除或改写全部 focusStats 断言（T-44 store 白名单、license entitlements、focus-widget 存在、renderFocusStats 等），改为“专注统计已移除”新语义断言（如 DEFAULT_SETTINGS 无 focusStats、index.html 无 focus-widget、license entitlements 无 focusStats 且 todos 仍为 true、双语文案无 featureFocusStats）；其余断言保持通过。
  - smoke.js：检查并清理 focus 相关引用（如有），保留其余测试。
- 验收标准：
  1. `npm run check` 全绿（含更新后的“专注统计已移除”断言）
  2. `npm run smoke` 全绿
  3. 专注统计无残留（面板/设置/逻辑/store/权益/文案/断言；仅“已移除”断言与历史注释允许提及）
  4. 待办与喝水提醒组件完整保留；存量 settings.json 的 focusStats 字段读写时被删除
  5. 未触碰禁止文件；无外部副作用；不合并 main、不 push
- 完成定义：实现并自检（check/smoke），更新本卡状态为“已完成（待协调者验收）”，提交分支；回报（分支/提交/改动文件清单/验证结果/残留说明）。
- 备注：worktree 已由协调者建好且 node_modules 已装；不要新建其他 worktree，不要改 main 工作区。

## 完成记录（worker 填写）

- 状态：已完成（已验收合并）
- 分支：codex/m4-focus-stats（基线 main 87cf5a5；提交号见本次回报）
- 实现摘要：
  1. index.html：删除 #focus-widget 专注统计卡片区块（含 focus-count/focus-minutes 与 focus.* 文案绑定）；账户/订阅 Pro 功能列表删除“专注统计”条目（license-feature-focus/license.featureFocusStats）；喝水/待办组件保留。
  2. chat.js：删除 elements 中 focusWidget/focusCount/focusMinutes 与 licenseFeatureFocus 引用；删除 todayKey/normalizeFocusStats/renderFocusStats/recordFocusSession 函数；设置加载/保存分支移除 focusStats；updateWidgetVisibility 与许可证门控移除专注统计接线；导出删除 getFocusStats；头部与相关注释同步移除“专注统计”字样；待办/喝水逻辑与导出保留。
  3. chat.css：删除 .focus-stats/.focus-stat/.focus-stat-value 样式与“专注统计”注释；喝水/待办样式保留；--t44-focus-ring（键盘焦点可见性，与专注统计无关）保留。
  4. store.js：DEFAULT_SETTINGS 移除 focusStats；删除 sanitizeFocusStats 及 merge/whitelist/patch 中 focusStats 分支；readSettings 对存量 settings.json 的 focusStats 字段执行 delete（不迁移、不暴露，同 ADR-038 番茄钟处理）。
  5. license.js：entitlements 移除 focusStats（todos 保留），注释同步。
  6. zh-CN/en.json：删除 focus.*（widgetAriaLabel/title/countLabel/minutesLabel/proOnly）与 license.featureFocusStats；待办/喝水文案保留。
  7. check.js：删除 focus-widget 存在、renderFocusStats/recordFocusSession 存在、focus.* 文案键、store focusStats 清洗、license entitlements.focusStats 等旧断言；新增“专注统计已移除”语义断言（index.html 无 focus-widget/license-feature-focus、chat.js 无 focus 专属函数与导出、DEFAULT_SETTINGS 无 focusStats、双语文案键已删、license entitlements 无 focusStats 且 todos 仍为 true、存量 focusStats 字段读写时删除）；其余断言保持通过。
  8. smoke.js：检查后无 focus 相关引用（未改动，保留其余测试）。
- 改动文件：src/renderer/index.html、src/renderer/chat.js、src/renderer/chat.css、src/storage/store.js、src/main/license.js、src/shared/locales/zh-CN.json、src/shared/locales/en.json、scripts/check.js、docs/tasks/T-51.md（本卡）；scripts/smoke.js 经检查无 focus 引用未改动。
- 验证结果：
  - `npm run check`：全部通过（含“专注统计已移除”新语义断言：index/chat.js/store/license/双语文案/存量字段清理）
  - `npm run smoke`：全部通过（渲染页加载、启动历史恢复、chat 表单端到端；无 focus 相关测试需删除）
- 遗留风险：
  - 存量用户 settings.json 中的 focusStats 字段在下一次读/写时被删除，不迁移、不兼容读取（ADR-039 预期行为）。
  - chat.js 顶部历史注释中的“T-44 效率小组件”表述保留（不再含专注统计）；无障碍 --t44-focus-ring 焦点环变量与 `:focus-visible` 样式保留（键盘可访问性，与专注统计无关）。
  - 未做 `npm run dev` 人工目检（本卡为无头执行）；UI 移除为静态断言 + smoke 渲染页加载覆盖。

## 验收合并记录（协调者填写）

- 复核：改动 9 个文件均在任务卡允许范围；未触碰 package.json/contracts.js/locales/index.js/orchestrator 等禁止文件；分支基于 87cf5a5，fast-forward 合并 1e9d73e。
- 验证：worker 与协调者 `npm run check`、`npm run smoke` 全绿（含“专注统计已移除”断言：index.html 无 focus-widget/license-feature-focus、chat.js 无 focus 专属函数与导出、DEFAULT_SETTINGS 无 focusStats、双语文案键已删、license entitlements 无 focusStats 且 todos 仍 true、存量 focusStats 字段读写时删除）；喝水/待办组件保留确认。
- 遗留：存量 settings.json focusStats 字段下次读写时删除（ADR-039 预期）；chat.js 历史注释与 --t44-focus-ring 焦点环保留（与专注统计无关）；无头执行，UI 已由 smoke 渲染页加载覆盖，可随 dev 目检复核。
