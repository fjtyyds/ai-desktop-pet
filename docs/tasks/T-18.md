# T-18 对话导出与数据清除

- 状态：已完成
- 分支：codex/m3x-export
- 工作树：E:\codex\AI桌宠-m3x-export
- 目标：会话导出 Markdown/JSON；一键清除本地数据（确认弹窗）
- 涉及文件：src/shared/contracts.js（history.export/clear，协调者冻结）、src/main/ipc.js、src/main/preload.js、src/renderer/chat.js、src/renderer/index.html、src/shared/locales
- 禁止触碰：package.json、src/llm/**
- 依赖：ADR-022；导出文件保存对话框（主进程 dialog）
- 验收：导出内容与历史一致；清除后历史/记忆/设置按选项清空并确认；check/smoke 通过
- 完成定义：提交到 codex/m3x-export

## 说明

- 本卡内容原为 T-20（用户要求 T-18/T-20 内容互换，2026-08-09）。

## 完成记录（2026-08-09）

### 实现

- `src/main/ipc.js`：新增 `history:export` / `history:clear` 通道。
  - 导出：主进程 `dialog.showSaveDialog` 选择路径，支持 Markdown / JSON；
    Markdown 按历史顺序渲染用户/桌宠消息，JSON 直接输出与 `history.get`
    一致的归一化消息数组；对话框文案按当前语言本地化。
  - 清除：`dialog.showMessageBox` 确认弹窗（文案入 zh-CN/en 语言包），
    确认后按 `messages` / `memories` / `settings` / `all` 清空：
    messages.json、memories.json 写回空数组；settings.json 重置为默认值
    （保留未知扩展字段，不破坏结构）；同时重置聊天服务缓存。
- `src/main/preload.js`：暴露 `petAPI.history.export({ format })` 与
  `petAPI.history.clear({ scope })`，通道名与 ipc.js 一致。
- `src/renderer/index.html` / `chat.js`：设置页新增“导出对话”（Markdown /
  JSON 两个按钮）与“清除数据”（范围下拉 + 清除按钮）；导出/清除结果状态提示；
  清除聊天记录或全部后刷新聊天视图并回到空态问候，清除设置或全部后重载设置表单。
- `src/shared/locales/zh-CN.json` / `en.json`：新增 `data.*` 文案（导出、
  清除范围、确认弹窗、成功/失败/取消提示）。
- 未改动：contracts.js、API.md、check.js、package.json、src/llm/**、PLAN/STATUS。

### 验证

- `node --check`：ipc.js / preload.js / chat.js 通过。
- `npm run check`：通过。
- `npm run smoke`（真实用户权限）：通过（渲染页加载、历史恢复、聊天端到端）。
- 导出内容核对（Electron 临时脚本）：Markdown 含标题/角色/全部消息且顺序与历史一致；
  JSON 文件内容与 `history.get` 数组完全一致；实际写文件与返回内容一致。
- 清除范围核对：`messages` 仅清空聊天记录、`memories` 仅清空长期记忆、
  `settings` 重置为默认值且 persona/结构完整、`all` 全部清空且 settings.json
  结构不被破坏。
- UI 离屏检查：导出/清除控件存在、zh/en 本地化生效、`petAPI.history.export/clear`
  已暴露、清除范围 4 项齐全。

### 完成定义

- 已提交到 `codex/m3x-export` 分支；未合并 main（由协调者集成）。
