# T-19 窗口体验（贴边隐藏/位置记忆/全局快捷键）

- 状态：已完成
- 分支：codex/m3x-window
- 工作树：E:\codex\AI桌宠-m3x-window
- 目标：拖到屏幕边缘自动贴边半隐藏、鼠标靠近滑出；记住窗口位置重启恢复；全局快捷键呼出
- 涉及文件：src/main/main.js、src/main/preload.js、src/shared/contracts.js（window 契约，协调者冻结）、src/storage/store.js（位置与快捷键设置）、src/renderer/chat.js（提示）
- 禁止触碰：package.json、src/llm/**
- 依赖：ADR-022；globalShortcut 冲突处理
- 验收：贴边/滑出/位置恢复/快捷键可用且可关闭；check/smoke 通过
- 完成定义：提交到 codex/m3x-window

## 完成记录（2026-08-10）

- 实现：贴边隐藏/鼠标滑出、窗口位置记忆与恢复、全局快捷键（Ctrl+Alt+P，冲突自动换备用键）、设置页开关（dockEnabled/shortcutEnabled，store 白名单登记 windowBounds）。
- 提交说明：实现由被中断的 codex exec 会话产生（未提交），协调者验证 node --check / npm run check / npm run smoke 全部通过后提交。
- 复查修复：端到端验证发现 Windows 上程序化移动（setPosition/setBounds）只触发 `move` 不触发 `moved`，位置可能漏存；补充 `move` 防抖落盘（500ms，贴边/动画期间跳过）并在 `closed` 时清理定时器。验证：契约暴露、设置页开关、快捷键开关往返、位置落盘、重启位置恢复与开关持久化全部通过；check/smoke 通过。
- 补充修复：位置记忆在贴边关闭时仍生效；Windows 程序化移动不触发 moved 事件，改用 move 事件 500ms 防抖落盘；双阶段重启验证（移动窗口→退出→重启恢复）通过。
- 协调循环复查（2026-08-10）：分支已有实现且任务卡为“已完成”，本轮按验收标准复验——`node --check`（四个涉及文件）、`npm run check`、`npm run smoke`（真实用户权限）全部通过；纯 Node 复测 dockEnabled/shortcutEnabled/windowBounds 默认值、开关往返、取整、null 重置与非法值清洗均通过；无需代码改动。

## 协调记录（orchestrator）

- 2026-08-09T22:14:17.673Z：验收通过并合并（分支 codex/m3x-window，提交 40d4a7d）。
