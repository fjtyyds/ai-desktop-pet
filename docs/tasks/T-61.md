# T-61 设置与引导：浮窗设置块完善 + 首次开启引导气泡

- 状态：待派发（2026-08-13）
- 分支：codex/m5x-settings；工作树：E:\codex\AI桌宠-m5x-settings（总工创建，node_modules 为 junction）
- 目标：设置页“宠物浮窗”从单开关变为完整设置块（气泡开关/时长/提醒透出），首次开启时浮窗给出引导气泡。
- 涉及文件（只允许改这些）：
  - src/renderer/index.html
  - src/renderer/chat.js
  - src/renderer/chat.css
  - src/shared/locales/zh-CN.json、en.json
  - scripts/check.js、scripts/smoke.js
  - docs/tasks/T-61.md（本卡）
- 只读文件：src/storage/store.js（ADR-045 已预冻结字段）、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、src/main/**、src/renderer/overlay.*、src/main/pet-overlay.js
- 实现要求：
  1. 设置页外观组“宠物浮窗”区块扩展：气泡显示开关（petOverlayBubbleEnabled）、气泡时长数字输入（petOverlayBubbleSeconds，3~20，保存后生效）、提醒透出开关（petOverlayReminders）；全部走既有 settings.get/set，不新增 IPC。
  2. 首次开启浮窗（petOverlayEnabled 由 false→true 且此前未引导过，本地标记 localStorage `petOverlayGuided`）时，通过 petAPI.petOverlay.pushBubble({state:'attention', text: 引导文案}) 显示“我已出现在桌面右下角”；此后不再重复。
  3. 文案完善：说明浮窗可拖动、可右键、`/pet` 命令。
  4. 表单回填与保存逻辑接入 restoreSettings/saveSettings；清数据时 localStorage 标记一并清除。
- 验收标准：
  1. npm run check 全绿（新增 T-61 断言：三个控件 id、保存接线、引导标记、双语文案）。
  2. npm run smoke 全绿（含回填与保存端到端）。
  3. 未触碰主进程与 store.js；契约只读。
- 完成定义：实现并自检 → 更新本卡状态 → 提交分支 → 回报。
