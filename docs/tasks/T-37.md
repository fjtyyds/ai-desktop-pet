# T-37 自动更新（electron-updater + GitHub Releases）

- 状态：未开始（2026-08-10，ADR-031）
- 分支：codex/m3x-autoupdate（建议）
- 工作树：E:\codex\AI桌宠-m3x-autoupdate（建议）
- 目标：正式版支持自动更新：启动后后台检查 GitHub Releases，发现新版本弹窗确认，下载后重启安装；托盘菜单“检查更新”；开发模式不检查、失败静默记录。
- 涉及文件：
  - src/main/updater.js（新增：封装 autoUpdater，checkForUpdates/on-update/下载进度/失败日志）
  - src/main/main.js（app ready 后按 isPackaged 初始化 updater；退出前 quitAndInstall 处理）
  - src/main/tray.js（菜单项“检查更新”/“Check for Updates”，双语）
  - src/shared/locales/zh-CN.json、en.json（更新文案）
  - scripts/check.js（断言：updater 模块存在、isPackaged 守卫、托盘菜单接入、双语文案）
  - docs/tasks/T-37.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md（本任务无契约变更）、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json（协调者已安装 electron-updater@6.8.9）、electron-builder.yml（publish 已由协调者配置）、scripts/orchestrator/**
- 依赖：electron-updater@6.8.9 已安装；publish 配置已就绪；CI release 工作流已就绪

## 验收标准

1. `app.isPackaged === false` 时绝不检查更新
2. 打包后检查 GitHub Releases（feed 来自 electron-builder 生成的 app-update.yml）；当前无更新/失败均静默或仅日志，不崩溃、不阻塞启动
3. 发现更新：弹原生 dialog 确认 → 下载（显示进度或等待）→ 下载完成提示重启安装（quitAndInstall）
4. 托盘菜单“检查更新”可用，双语文案正确
5. `npm run check` 通过；`npm run smoke` 通过或说明原因
6. 无法在本机实测真实更新（无已发布版本）时，写明验证策略（如 mock/代码走查），不得伪造通过

## 完成定义

实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不自行 rebase；完成后回报协调者。
