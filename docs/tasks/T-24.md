# T-24 窗口布局与可缩放

- 状态：已完成（待协调者验收）
- 分支：codex/m3x-fix-layout
- 工作树：E:\codex\AI桌宠-m3x-fix-layout
- 目标：窗口可缩放；小部件不遮挡聊天；移除底部平台/版本信息
- 涉及文件：
  - src/main/main.js（BrowserWindow 增加 resizable/minWidth/minHeight，保留 frame:false）
  - src/renderer/index.html（布局调整、移除 meta footer）
  - src/renderer/chat.css（响应式布局、小部件默认收起/可折叠）
  - src/renderer/chat.js（小部件折叠状态、布局初始化）
  - src/renderer/renderer.js（移除平台/版本写入）
  - src/shared/locales/zh-CN.json、en.json（如移除 platformVersion 文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：无（若 T-30 先移除系统状态小部件，本卡只需处理天气小部件）
- 验收标准：
  1. 窗口可缩放且有合理最小尺寸（建议 ≥280×360），聊天区随窗口自适应
  2. 天气小部件打开时不遮挡消息区和输入框（默认收起或可折叠）
  3. 界面底部不再显示“平台/版本”信息
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因（需真实用户权限）
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。

## 完成记录（2026-08-10）

- 分支：codex/m3x-fix-layout；提交号：ab0b99e。
- 实现：
  - src/main/main.js：BrowserWindow 改为 resizable:true，新增 minWidth/minHeight（280×360），
    位置记忆扩展为位置+尺寸记忆，恢复时按最小尺寸钳制。
  - src/renderer/index.html：移除底部 `<footer class="meta">`；天气小部件新增折叠按钮并默认收起。
  - src/renderer/chat.css：天气收起/展开样式；小窗口（max-height:480px）自适应，
    高度不足时隐藏小部件面板，优先保证聊天区与输入框可用。
  - src/renderer/chat.js：新增天气折叠状态/交互（weatherCollapsed、applyWeatherCollapsed、
    toggleWeatherCollapsed）；移除 applyMeta 平台/版本写入。
  - src/renderer/renderer.js：移除平台/版本写入。
  - src/shared/locales/zh-CN.json、en.json：移除 meta.platformVersion，新增 weather.collapse/expand。
  - scripts/check.js：新增 T-24 断言（可缩放+最小尺寸、底部信息移除、天气可折叠）。
- 自检：`npm run check` 通过（含新增断言）；`npm run smoke` 通过
  （沙箱内因 Electron 无法写 AppData/GPU 崩溃，改以真实用户权限运行成功，与文档记录一致）。
- UI 目检：以离屏渲染+布局断言完成自查——280×360 收起（消息区 217px、输入框完整可见）、
  280×360 展开（消息区 168px、输入框完整可见）、440×640 展开均通过；
  截图存于可视化输出目录。本环境无法直接目视图片，建议协调者/用户再用 `npm run dev` 人工复核。
- 边界外触碰：无；未修改只读/禁止文件，package.json/package-lock.json 未变，契约未改。
