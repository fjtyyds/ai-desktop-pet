# T-55 宠物浮窗：Codex Pets 式独立悬浮宠物 + 状态气泡 + 宠物包导入

- 状态：已完成（待协调者合并，2026-08-13）
- 分支：codex/m5-pet-overlay（工作树 E:\codex\AI桌宠-m4-newuser 复用，基线 main efa09c5）
- 目标：把 OpenAI Codex 的“显示宠物”功能（独立悬浮宠物 + 工作状态气泡 + 自定义宠物包）落地到 AI 桌宠。
- 背景（用户需求）：用户见 Codex 有“显示宠物”功能（Settings → Appearance → Pets；`/pet` 切换；宠物显示运行中/等待输入/待审阅状态；自定义宠物为 ~/.codex/pets 下 pet.json + spritesheet.webp），希望 AI 桌宠也具备。ADR-044 已落盘。
- 涉及文件：
  - 新增：src/main/pet-overlay.js、src/renderer/overlay.html、src/renderer/overlay.css、src/renderer/overlay.js、docs/tasks/T-55.md
  - 修改：src/main/main.js、src/main/tray.js、src/main/preload.js、src/main/skin-store.js、src/storage/store.js、src/shared/contracts.js、src/renderer/index.html、src/renderer/chat.js、src/renderer/chat.css、src/shared/locales/zh-CN.json、src/shared/locales/en.json、scripts/check.js、scripts/smoke.js、docs/DECISIONS.md、docs/SPEC.md、docs/STATUS.md、PLAN.md、docs/reports/2026-08-11-皮肤包格式.md
- 实现要求：
  1. 独立浮窗：240×320 透明置顶小窗，仅角色 + 状态气泡 + 收起按钮；位置持久化；主窗口隐藏时仍可见。
  2. 入口：设置页外观组“宠物浮窗”开关 + “显示/隐藏宠物”按钮；托盘菜单“显示宠物/隐藏宠物”；聊天框 `/pet` 命令。
  3. 状态：idle/working/ready/failed，聊天页发送/完成/失败时上报；气泡文案双语文案由浮窗本地化。
  4. 宠物包：skin-store 支持 Codex 宠物包（pet.json + spritesheet.webp，8 列×9 行，单元格 64~512px），安全校验沿用 T-43 边界（仅 .png/.json/.webp、≤10MB、≤50 条目、无路径跳转/加密/符号链接）。
  5. 契约：preload 暴露 petAPI.petOverlay.*；contracts.js 补充说明。
  6. 验证：npm run check 全绿（含 WebP 尺寸解析与宠物包导入/拒绝运行时断言）；npm run smoke 全绿（含浮窗端到端断言）；离屏像素验证浮窗气泡与角色区域有实际渲染。
- 完成定义：实现并自检（check/smoke），更新本卡状态，提交分支，由协调者验收合并。

## 完成记录（2026-08-13）

- 实现摘要：
  - pet-overlay.js：窗口生命周期、位置持久化、状态机、IPC（get-status/set-status/get-skin/toggle-overlay/set-enabled/tuck-away/refresh-skin）、托盘联动；IPC 提前注册保证浮窗未显示时聊天页也能上报。
  - overlay.html/css/js：状态气泡、8 列×9 行图集按状态切换动画行（idle=0/working=7/ready=8/failed=5）、静态皮肤浮动动效、ready/failed 6 秒回落 idle、收起按钮、皮肤变更事件订阅。
  - skin-store.js：允许 .webp；parseWebpSize（VP8X/VP8/VP8L）；pet.json 清单解析；8×9 网格与单元格尺寸校验；atlas 条目（kind/spritesheetDataUrl/atlas）；导入/列表/导出/移除兼容。
  - main.js/tray.js：启动按 petOverlayEnabled 显示；托盘菜单项；退出销毁。
  - index.html/chat.js/chat.css：外观组开关与入口；`/pet` 命令；状态上报；标题栏与皮肤列表图集裁切动画；皮肤应用后刷新浮窗。
  - store.js：petOverlayEnabled（默认 false）与 petOverlayBounds 白名单/清洗。
  - locales：overlay.*、settings.petOverlay*、skin.animated、tray.showPet/hidePet（zh-CN/en）。
  - check.js/smoke.js：静态断言 + WebP 解析/宠物包导入/非法图集拒绝运行时断言 + 浮窗端到端断言。
- 验证结果：
  - npm run check 全绿（T-55 静态断言 + 运行时断言通过）。
  - npm run smoke 全绿（浮窗状态/皮肤/气泡端到端通过）。
  - 离屏渲染像素验证：气泡区域 12000 不透明像素、角色区域 44000 不透明像素（303×403 物理像素截图，overlay-visual.png 留存于 outputs/）。
- 残留说明：
  1. 浮窗点击/双击暂未绑定“唤起主窗口”动作（后续可扩展）。
  2. 内置 3 套皮肤为静态 PNG；动画效果需导入 Codex 宠物包（hatch-pet 等产物）体验。
  3. 工作树复用 T-54 的 E:\codex\AI桌宠-m4-newuser（node_modules 为 junction），符合“独立 worktree + 分支”纪律。
