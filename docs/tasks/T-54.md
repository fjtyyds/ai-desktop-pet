# T-54 新用户版本收口：移除沙箱/测试痕迹 + 首次引导可达（含重新查看入口）

- 状态：已完成（待协调者验收，2026-08-11）
- 分支：codex/m4-newuser
- 工作树：E:\codex\AI桌宠-m4-newuser（协调者已创建，基线 main f037958；node_modules 为 junction 链接，勿 npm install）
- 目标：生成“给新用户”的生产版本——终端用户界面不得残留开发/测试痕迹（沙箱支付、mock 激活码等桩 UI），首次启动三步引导对真正新用户必须完整可达，并让已有用户也能从设置页重新查看引导；验收后由协调者构建并替换 E:\codex\AI桌宠最新版。
- 背景（协调者已核实）：
  1. 首次引导功能（T-20）已在 main：DEFAULT_SETTINGS.onboardingDone=false、index.html 含 onboarding-view，真正全新安装会显示。用户装完看不到，是因为其机器已有旧 userData（onboardingDone=true），覆盖安装不触发。
  2. 生产 UI 可见测试痕迹：设置页“沙箱支付（未接入真实网关）”区块（#payment-plans：两个“沙箱购买”按钮 + “仅模拟支付流程…”notice）；license 激活区（#license-code/#license-activate）为本地 mock 校验的开发桩（license.js 为“可 mock 的激活码/订单号校验”+ dev- 前缀设备 id）。
  3. scripts/check.js 当前断言 index.html 必须包含 payment-plans/payment-buy-*/payment-message/license-code/license-activate 等 token，需同步改造为“不得包含”防回归断言。
  4. scripts/smoke.js 已用临时 userData（全新档案），适合新增“新用户首启引导可见”断言。
- 涉及文件（只允许改这些）：
  - src/renderer/index.html
  - src/renderer/chat.js
  - src/renderer/chat.css（如新增占位卡片/按钮样式需要）
  - src/shared/locales/zh-CN.json
  - src/shared/locales/en.json
  - scripts/check.js
  - scripts/smoke.js
  - docs/tasks/T-54.md（本卡）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/main/**、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡外）；不合并 main、不 push、不运行 sync-latest.ps1（协调者负责）。
- 实现要求：
  1. 移除生产 UI 中的开发/测试痕迹：
     a. 设置页沙箱支付区块整体移除，替换为干净占位（如“Pro 会员即将上线”）；不得再出现“沙箱/模拟支付/未接入真实网关”等字样。
     b. license 激活区（激活码/订单号输入、激活/停用按钮）属于无后端开发桩，一并替换/隐藏为同一占位；删除 renderer 中 activateLicense/deactivateLicense/sandboxPurchase 流程与按钮事件绑定、相关 UI 刷新与 status 逻辑。
     c. 清理因此不再使用的 payment.* / license.* locale keys（zh-CN.json/en.json 同步删除，避免死文案）；主进程 src/main/payment.js、src/main/license.js 及其 check 断言保持不动（内部桩留给未来真实支付接入，不暴露 UI）。
     d. 全仓 UI 可见字符串复查：不得残留“沙箱/模拟支付/未接入真实网关”等测试文案；“不填写 API Key 时将以演示模式运行”属正式演示模式文案，可保留，需在完成记录中说明。
  2. 首次引导对真正新用户必须可达且可完成：
     a. 不得改变 store.js 的 onboardingDone 默认值/持久化语义（默认 false 已核实）。
     b. scripts/smoke.js 新增新用户首启断言：临时 userData 下 onboarding-view 存在且未隐藏、默认设置 onboardingDone=false。
     c. 设置页新增“重新查看新手引导”入口（按钮）：点击立即显示三步引导覆盖层（不重置语言/API Key/人格等已有设置；完成时照常持久化 onboardingDone=true）；双语文案。
  3. scripts/check.js 同步：删除旧“必须包含”断言（payment-plans/payment-buy-*/payment-message/license-code/license-activate 等），新增防回归断言（index.html 不得包含沙箱支付 UI token；必须包含重新查看引导入口 token）。
  4. 验证：npm run check 全绿、npm run smoke 全绿（含新增首启断言）；如环境可行，用临时 userData 启动真窗口截图确认引导层可见；若不可行须说明，由协调者验收时补目检。
- 验收标准：
  1. npm run check 全绿（新旧断言均通过）
  2. npm run smoke 全绿（含新用户首启引导断言）
  3. index.html/locales 无任何 sandbox/沙箱支付字样；renderer 无 sandboxPurchase/activateLicense/deactivateLicense 残留（函数与绑定）；主进程未改动
  4. 设置页重新查看引导入口存在且可触发（静态断言 + 可行时真窗口验证）
  5. 未触碰禁止文件；无外部副作用；不合并 main、不 push
- 完成定义：实现并自检（check/smoke），更新本卡状态为“已完成（待协调者验收）”，提交分支；回报（分支/提交/改动文件清单/删除与新增断言清单/验证结果/残留说明）。

## 完成记录（worker 填写）

### 2026-08-11 完成（待协调者验收）

- 分支：codex/m4-newuser（工作树 E:\codex\AI桌宠-m4-newuser，基线 main f037958）
- 改动文件：
  - src/renderer/index.html：移除沙箱支付区块与 mock 激活区（payment-plans/payment-buy-*/payment-message/license-code/license-activate/license-deactivate/license-message/account-upgrade-btn），替换为干净占位 #pro-coming-soon（“Pro 会员即将上线”，双语文案），新增 #review-onboarding-btn（“重新查看新手引导”）
  - src/renderer/chat.js：删除 activateLicense/deactivateLicense/hasPaymentApi/sandboxPurchase 函数、相关元素缓存与事件绑定、showLicenseMessage/showPaymentMessage 与 window.ChatUI.sandboxPurchase 导出；新增 reviewOnboardingBtn 缓存与点击绑定（点击复用 showOnboarding 显示三步引导，不重置已有设置）
  - src/renderer/chat.css：删除 .license-actions/.payment-*/.account-upgrade-btn 样式，新增 .pro-coming-soon 占位卡片样式
  - src/shared/locales/zh-CN.json、en.json：删除 payment.* 全部键与 license 激活相关键（activateLabel/activatePlaceholder/activateButton/deactivateButton/activating/activated/activateError/deactivated/deactivateError）及 settings.upgradeActivate；新增 license.comingSoonTitle/comingSoonHint 与 settings.reviewOnboarding
  - scripts/check.js：删除旧“必须包含”断言（payment-plans/payment-buy-*/payment-message/license-code/license-activate/license-deactivate/license-message/account-upgrade-btn/upgradeActivate 等），新增防回归断言（index.html 不得包含测试桩元素、chat.js 不得含 sandboxPurchase/activateLicense/deactivateLicense/showLicenseMessage/showPaymentMessage、chat.css 不得含 payment 样式、index.html/zh-CN.json/en.json 不得含沙箱/模拟支付/未接入真实网关字样；必须包含 review-onboarding-btn 与 pro-coming-soon；locales 必须含 comingSoonTitle/comingSoonHint/reviewOnboarding）
  - scripts/smoke.js：新增全新档案首启断言（onboarding-view 存在且未隐藏、默认 onboardingDone=false，等待 restoreSettings 完成后再断言）
  - docs/tasks/T-54.md：本卡完成记录
- 验证结果：
  - npm run check 全绿（含 T-54 新防回归断言与既有 license/payment 主进程桩测试）
  - npm run smoke 全绿（含新用户首启引导断言）
  - 真窗口验证（临时 userData）：全新档案首启引导层可见、onboardingDone=false；预置 onboardingDone=true 的档案中点击“重新查看新手引导”立即显示引导覆盖层，语言(en)/API Key(sk-test-preserved)未重置，完成引导后 onboardingDone=true 正常持久化；截图保存于 C:\Users\HP\.codex\visualizations\2026\08\11\019ff0ae-ca8f-76a3-89ce-afcf0ab3e371\onboarding-newuser.png 与 onboarding-review.png
- 残留说明：
  1. 主进程 src/main/payment.js、src/main/license.js、IPC/preload 内部桩与 check.js 主进程断言按任务要求保留（不暴露 UI，供未来真实支付接入）。
  2. store.js onboardingDone 默认值/持久化语义未改动（默认 false）；“不填写 API Key 时将以演示模式运行”属正式演示模式文案，按要求保留。
  3. src/shared/locales/index.js 注释含“沙箱渲染进程”字样（禁止触碰文件，非 UI 可见字符串，未改动）。
  4. 未合并 main、未 push、未运行 sync-latest.ps1、未改动禁止清单文件。

## 验收合并记录（协调者填写）

### 2026-08-11 验收通过并合并（新总工 019ff0b4）

- 验收门禁：npm run check 全绿（含 T-54 防回归断言与既有主进程桩断言）；npm run smoke 全绿（含新用户首启引导断言）；index.html/chat.js/chat.css/locales 静态复查无沙箱/模拟支付/激活桩 token（rg 零命中）；主进程与禁止清单文件未改动；仅白名单 8 文件有差异。
- worker 自检补充：真窗口验证全新档案首启引导可见、onboardingDone=false；已有用户点击“重新查看新手引导”立即显示覆盖层且语言/API Key 未重置，完成后 onboardingDone=true 正常持久化（截图 onboarding-newuser.png / onboarding-review.png）。
- 合并：codex/m4-newuser → main fast-forward 5da29f6；main `npm run check` 复检通过。
- 同步：scripts/sync-latest.ps1 构建成功，E:\codex\AI桌宠最新版 已更新（0.1.0 @ 5da29f6，NSIS 103.70MB / MSIX 143.49MB / msixupload 143.30MB，latest.json 已生成）。
- 待办：用户安装最新版验证（新用户引导 + 设置页重新查看入口 + 无测试痕迹）。
