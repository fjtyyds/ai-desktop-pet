# T-17 记忆管理页

- 状态：已完成
- 分支：codex/m3x-memory-ui
- 工作树：E:\codex\AI桌宠-m3x-memory-ui
- 目标：设置页新增“记忆”页：查看/删除/修正长期记忆
- 涉及文件：src/shared/contracts.js（memory.list/delete/update，协调者冻结）、src/main/ipc.js、src/main/preload.js、src/storage/memory-store.js（补 updateMemory）、src/renderer/index.html、src/renderer/chat.js、src/renderer/chat.css、src/shared/locales
- 禁止触碰：package.json、src/llm/**
- 依赖：ADR-022（契约冻结）、memory-store 已有能力
- 验收：记忆列表显示、删除生效且持久化、修正（更新内容）生效；UI 空态与错误提示；check/smoke 通过
- 完成定义：提交到 codex/m3x-memory-ui

## 完成记录（2026-08-09）

- 契约：按 API.md 第 5 节与 ADR-022 实现 `petAPI.memory.list/delete/update`；contracts.js 只读未改动。
- IPC（src/main/ipc.js）：新增 `memory:list`、`memory:delete`、`memory:update` 通道与处理器；delete/update 校验 id 与内容，返回 `{ ok, error? }`（`memory-invalid-id`/`memory-not-found`/`memory-empty-content`），异常不外抛。
- preload：暴露 `petAPI.memory.{ list, delete, update }`。
- 存储（src/storage/memory-store.js）：新增 `updateMemory(id, { content })`，更新内容与 updatedAt、保留 lastUsedAt，落盘持久化；空内容抛错、不存在返回 null。
- UI（index.html/chat.js/chat.css）：设置页新增“记忆管理”入口与“记忆”子页；记忆卡片支持查看、内联修正（保存/取消）、删除；空列表显示空态文案；加载/删除/修正失败显示错误提示；语言切换时列表文案随 locale 重渲染。
- i18n：zh-CN.json / en.json 增加 memory* 文案（记忆、管理入口、空态、错误、按钮、更新时间等）。
- 校验：
  - `node --check` 通过（memory-store/ipc/preload/chat）；
  - `npm run check` 全部通过；
  - `npm run smoke`（真实用户权限）通过；
  - 纯 Node 复核：add → 重启重读 → update 持久化 → delete 持久化 → 空内容抛错/不存在返回 null/false 均通过；
  - Electron 临时端到端（已删除脚本）：列表显示 2 条 → 修正内容生效 → 逐条删除 → 空态显示 → 强制 list 抛错后错误提示显示且不崩溃。
- 未合并 main；分支提交后由协调者处理合并与 PLAN/STATUS 更新。
