# T-28 人格模板文案精简

- 状态：已完成（待协调者验收）
- 分支：codex/m3x-persona-copy
- 工作树：E:\codex\AI桌宠-m3x-persona-copy
- 目标：人格模板文字描述过多、显得啰嗦；卡片只显示“名称 + 一句话”，完整描述折叠或选中后展开
- 涉及文件：
  - src/renderer/chat.js（PERSONA_TEMPLATES 文案、renderTemplateCards 卡片渲染）
  - src/renderer/chat.css（卡片、折叠/展开样式）
  - src/renderer/index.html（模板卡片容器结构）
  - src/shared/locales/zh-CN.json、en.json（如模板文案迁移到 locale）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：无
- 验收标准：
  1. 6 套模板卡片均只显示名称 + 一句话简介，不再整段铺开
  2. 完整人格描述可通过展开/选中查看，应用后 persona 内容不变
  3. 设置页与首次引导第 3 步使用同一套精简卡片
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。

## 完成记录（2026-08-10）

- 实现：6 套模板卡片精简为“名称 + 一句话简介”（description 单行省略，完整文案存 title 提示）；完整人格详情（性格/语气/背景）默认折叠，选中卡片时在卡片内展开。
- 两处入口：设置页与首次引导第 3 步共用 `renderTemplateCards` 同一套精简卡片；引导第 3 步删除独立预览面板（index.html 的 onboarding-template-preview、chat.js 对应元素引用与 updateOnboardingTemplatePreview、chat.css 的 template-preview 样式、ONBOARDING_TEXTS 的 templatePreview 文案），避免与卡片内展开重复。
- persona 不变：`applyPersonaTemplate`/`finishOnboarding` 逻辑未动，应用模板后 traits/tone/backstory 与模板数据逐字一致。
- check：`npm run check` 通过（含新增 T-28 断言：卡片精简、折叠/展开样式、两处入口一致、应用后 persona 不变）。
- smoke：`npm run smoke` 通过（真实权限下运行，渲染加载、历史恢复、聊天端到端正常）。
- UI 目检：以 offscreen Electron + DOM/计算样式断言完成——6 张卡片、折叠卡高 60px 只含名称+一句话、选中卡展开 3 行详情、非选中详情隐藏、一句话无溢出、设置页模板列表可见；截图已生成于可视化目录。本线程无法直接查看图片，最终视觉效果建议协调者/用户以 `npm run dev` 人工复核。
- 边界：未触碰 contracts.js、docs/API.md、PLAN.md、STATUS.md、AGENTS.md、DECISIONS.md、package.json、locale 文件、store.js、scripts/orchestrator/**；临时目检脚本已删除未提交。
