# T-20 首次引导与预设人格模板

- 状态：已完成（2026-08-10）
- 分支：codex/m3x-onboarding-r2
- 工作树：E:\codex\AI桌宠-m3x-onboarding
- 目标：首次启动三步引导（语言/API Key 与模型/人格模板）；内置 5-6 套人格模板一键切换
- 涉及文件：src/renderer/index.html、src/renderer/chat.js、src/renderer/chat.css、src/storage/store.js（onboarding 标志与模板默认值）、src/shared/locales、src/shared/contracts.js（如需模板常量，协调者冻结）
- 禁止触碰：package.json、src/llm/**
- 依赖：ADR-020；模板与现有 persona 字段对齐
- 验收：首次启动出现引导，完成后不再出现；模板切换即时生效并持久化；check/smoke 通过
- 完成定义：提交到 codex/m3x-onboarding

## 说明

- 本卡内容原为 T-18（用户要求 T-18/T-20 内容互换，2026-08-09）。

## 实现记录（2026-08-10，第 2 次尝试）

- 首次启动三步引导（语言 / API Key 与模型 / 人格模板）：
  - 新增 `onboardingDone` 设置字段（store.js 白名单 + 布尔清洗），完成后持久化，
    重启不再出现；
  - 引导覆盖层不阻塞底层历史恢复/聊天 DOM 行为，冒烟测试（含预置历史）通过。
- 内置 6 套人格模板（warm/sage/playful/gentle/cool/curious）：
  - 模板内容为 renderer/chat.js 内联中英双语表，id 与 `store.js PERSONA_TEMPLATE_IDS`/
    `DEFAULT_PERSONA_TEMPLATE_ID` 对齐，字段与现有 Persona（traits/tone/backstory，ADR-011）一致；
  - 设置页新增“人格模板”区，点击卡片即时调用 `petAPI.settings.set` 保存 persona 与
    `personaTemplate`，持久化并影响后续对话；手动微调后模板 id 回退为空（自定义）；
  - 引导第 3 步支持模板卡片选择与人格预览。
- 边界说明：本轮按要求未修改 `src/shared/locales/en.json`、`src/shared/locales/zh-CN.json`
  （上次失败原因）；引导与模板文案改由 chat.js 内联双语常量提供（沿用 T-19/T-23 模式），
  模板内容与现有 persona 字段对齐。
- 校验：`npm run check` 通过；`npm run smoke` 通过（需真实用户权限）；
  临时端到端脚本验证通过：首次显示引导 → 语言即时切换 → 三步完成并持久化 →
  重启不再出现 → 设置页一键切换模板并持久化；纯 Node 复测 onboardingDone/personaTemplate
  默认值、往返与非法值清洗通过。

## 协调记录（orchestrator）

- 2026-08-09T23:14:52.995Z：验收通过并合并（分支 codex/m3x-onboarding-r2，提交 b2cf869）。
