# T-58 情绪与动画联动：mood → waving/jumping + reduceMotion 同步 + waiting 行

- 状态：待派发（2026-08-13）
- 分支：codex/m5x-mood；工作树：E:\codex\AI桌宠-m5x-mood（总工创建，node_modules 为 junction）
- 目标：浮窗动画真正“活起来”：情绪映射动画行（happy→waving 行3、excited→jumping 行4），等待输入用 waiting 行6，减弱动效设置同步到浮窗。
- 涉及文件（只允许改这些）：
  - src/renderer/overlay.js
  - src/renderer/overlay.css
  - src/main/pet-overlay.js
  - src/shared/locales/zh-CN.json、en.json（如需新增）
  - scripts/check.js、scripts/smoke.js
  - docs/tasks/T-58.md（本卡）
- 只读文件：src/storage/store.js、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、src/renderer/chat.js、src/renderer/index.html、src/renderer/chat.css、src/main/ipc.js、src/main/main.js、src/main/preload.js（如确需新通道先回报总工冻结）
- 实现要求：
  1. 浮窗每 3s 轮询 petAPI.mood.get（已有通道），把 mood（valence/intensity）映射为动画行：excited/happy → waving（行3）或 jumping（行4），neutral → idle（行0），sad → failed（行5 淡入淡出或 idle 慢速）；状态气泡不受影响。
  2. 图集行映射补全：waiting（行6）用于 attention/等待输入；ready 用 review（行8）；working 用 running（行7）。
  3. 读取 settings.reduceMotion（通过 settings.get）应用到浮窗：true 时禁用图集/静态动画（尊重 prefers-reduced-motion 仍保留）。
  4. 静态皮肤：情绪兴奋时轻微浮动+表情气泡（如 🥳），文案双语文案。
- 验收标准：
  1. npm run check 全绿（新增 T-58 断言：行映射表、mood 轮询、reduceMotion 接线）。
  2. npm run smoke 全绿（注入假 mood 验证行切换，冒烟可暴露 window 测试钩子）。
  3. 不动契约文件；如需要新 IPC 先停手回报。
- 完成定义：实现并自检 → 更新本卡状态 → 提交分支 → 回报。
