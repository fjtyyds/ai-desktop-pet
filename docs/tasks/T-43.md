# T-43 皮肤与配件市场 MVP

- 状态：已完成（待协调者验收）（2026-08-11，ADR-032 上线方案）
- 分支：codex/m4-skins（建议）
- 工作树：E:\codex\AI桌宠-m4-skins（建议）
- 目标：本地皮肤包 MVP：皮肤包格式（JSON 清单 + PNG 资源，zip 或目录导入导出）、预览/启用/移除、内置 2~3 套默认皮肤、商店页框架（后续接分成支付）。皮肤包不包含可执行代码（安全边界）。
- 涉及文件：
  - src/main/skin-store.js（新增：皮肤包导入/导出/校验/索引）
  - src/main/ipc.js、preload.js（skin:list/import/export/apply 通道）
  - src/renderer/chat.js、index.html、chat.css（“皮肤”设置页/商店页框架）
  - src/storage/store.js（skinId 白名单字段）
  - src/shared/locales/zh-CN.json、en.json（皮肤文案）
  - scripts/check.js（断言：格式校验、无代码执行、导入导出往返）
  - docs/tasks/T-43.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md、docs/reports/2026-08-11-商业化上线方案.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、docs/**（除本卡外）
- 依赖：无
- 验收标准：
  1. 皮肤包格式文档化；导入非法包（缺清单/超大小/含可执行文件）拒绝且不崩溃
  2. 2~3 套默认皮肤可切换；预览图与角色资源正确加载
  3. 皮肤包导出→重导入往返一致
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push；回报协调者。

## 完成记录（2026-08-11）

- 实现：`src/main/skin-store.js`（纯 Node zip 读写 + 格式校验 + 索引/导入/导出/卸载）+ `src/main/default-skins/` 3 套内置皮肤（default/star/bunny，PNG 占位资源程序生成）。
- 通道：`skin:list/import/export/apply/remove`（ipc.js + preload.js → `petAPI.skin.*`）。
- 渲染层：设置页“皮肤与配件”子页（列表/预览/应用/导入/导出/移除），标题栏 `pet-avatar` 随 `settings.skinId` 切换默认形象（T-44 再做动效）。
- 存储：`settings.skinId` 白名单（≤64 字符，默认 `default`）。
- 文案：locales zh-CN/en 新增 `skin.*`（25 键）。
- 格式文档：`docs/reports/2026-08-11-皮肤包格式.md`（已列入允许范围）。
- 验证：
  - `npm run check` 通过（含新增 T-43 断言：内置皮肤齐全、非法包拒绝、往返一致、无代码执行机制、skinId 清洗）；
  - `npm run smoke` 通过；
  - 额外用例：缺清单/exe/js 脚本/超大小/路径跳转拒绝且不崩溃；目录导入→导出 zip→移除→重导入往返一致。
- 提交：`codex/m4-skins`（未合并 main、未 push）。

## 协调者验收（2026-08-11）

- 29548a4 经 merge 合并入 main（df99fa2，共享文件冲突由协调者解决）；main check/smoke 通过；worktree 已清理，分支保留。
