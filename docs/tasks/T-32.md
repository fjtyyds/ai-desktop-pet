# T-32 orchestrator 缺陷修复（最后执行）

- 状态：未开始（在所有优化任务完成后执行）
- 分支：无（scripts/orchestrator/ 未入库，直接在 main 工作区修改；是否入库待用户决定）
- 工作树：无（或用户决定先入库后另开分支）
- 目标：修复 orchestrator 已知缺陷并补测试
- 涉及文件：
  - scripts/orchestrator/orchestrate.mjs（多 --task 参数解析、token/时间预算统计、失败转人工阈值）
  - scripts/orchestrator/README.md（参数与预算说明）
  - scripts/orchestrator/config.example.json（预算默认值）
  - scripts/orchestrator/test/selftest.mjs、test/mock-worker.mjs（多任务参数、预算、失败转人工场景）
- 只读文件：docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/**、docs/**（除本卡完成记录外）
- 依赖：T-24~T-31 全部验收完成后才执行
- 验收标准：
  1. `--task` 可重复且全部生效（不再只认最后一个）
  2. 每次 codex exec 尝试记录耗时与输出量；支持按任务限额；连续失败达到阈值自动转人工
  3. `node orchestrate.mjs selftest` 通过，覆盖多任务参数与预算场景
  4. doctor/dry-run 对真实项目正常
  5. 修复结果与是否入库由用户决定，入库前不提交到 main
- 完成定义：修复并自测，更新本卡状态为“已完成（待协调者/用户验收）”；回报协调者。
