# T-59 皮肤体验：Codex pets 目录扫描导入 + 9 行状态预览 + 错误可读化

- 状态：已完成（待总工验收）（2026-08-13）
- 分支：codex/m5x-skin；工作树：E:\codex\AI桌宠-m5x-skin（总工创建，node_modules 为 junction）
- 目标：像 Codex 一样“刷新自定义宠物”——一键扫描本地 Codex pets 目录批量导入；皮肤列表展示图集 9 行状态预览；导入失败原因可读。
- 涉及文件（只允许改这些）：
  - src/main/skin-store.js
  - src/main/ipc.js
  - src/main/preload.js
  - src/renderer/chat.js
  - src/renderer/chat.css
  - src/shared/locales/zh-CN.json、en.json
  - scripts/check.js、scripts/smoke.js
  - docs/tasks/T-59.md（本卡）
- 只读文件：src/storage/store.js、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、src/main/pet-overlay.js、src/renderer/overlay.*、src/renderer/index.html、src/main/main.js
- 实现要求：
  1. 新增皮肤导入入口“扫描 Codex 宠物目录”：默认目录 `~/.codex/pets`（env CODEX_HOME 或 HOME/.codex 下）；用户也可自选目录；扫描子目录中 pet.json+spritesheet.webp 的合法包批量导入（复用 importPack 校验，逐包报错不中断）。
  2. 新增 IPC `skin:import-codepets`（可选参数 {path}），返回 {ok, imported[], failed[{name,error}]}；preload 暴露 petAPI.skin.importCodexPets。
  3. 皮肤列表图集预览改为 9 行小预览条（每行一个状态帧动画，或悬停切换行）；至少展示 idle/waiting/running/review/failed 5 行。
  4. 导入错误可读化：SkinError 的中文 message 直接展示，并在 UI 上分组显示失败原因（重复 id/尺寸不符/缺 spritesheet 等）。
  5. 安全边界不变：仅 .png/.json/.webp、≤10MB、≤50 条目、拒绝路径跳转/加密/符号链接；扫描目录本身不递归进 node_modules。
- 验收标准：
  1. npm run check 全绿（新增 T-59 断言：IPC/preload 接线、错误分组 UI token、安全边界未放宽）。
  2. npm run smoke 全绿（含临时 Codex pets 目录导入成功/失败分组端到端）。
  3. 未改 pet-overlay/overlay 页面；store/contracts 只读。
- 完成定义：实现并自检 → 更新本卡状态 → 提交分支 → 回报。

## 完成记录（2026-08-13）

- 实现摘要：
  - skin-store.js：新增 `scanCodexPetsDir`（目录自身是宠物包则直接导入，否则递归扫描子目录；跳过 node_modules/.git/隐藏目录与符号链接；逐包复用 importPack 校验，失败不中断，返回 {imported, failed}）；新增 `defaultCodexPetsDir`（env CODEX_HOME 优先，否则 HOME/.codex/pets）。
  - ipc.js/preload.js：新增 `skin:import-codepets` 通道与 `petAPI.skin.importCodexPets`；缺省优先扫默认目录，目录不存在时弹出目录选择框供自选；可选 {path} 供自动化。
  - chat.js：皮肤页动态注入“扫描 Codex 宠物目录”按钮与结果面板（index.html 只读，不改静态页）；扫描结果成功数 + 失败原因分组展示（`groupSkinImportFailures` 按错误文案合并）；图集皮肤列表改为 9 行状态预览条（每行一帧动画循环，含双语 title/aria-label）。
  - chat.css：`.skin-preview-atlas-rows` / `.skin-preview-atlas-row` / `pet-row-strip` 动画与结果面板样式。
  - locales：新增 skin.scanCodexPets*/previewRow* 双语文案（zh-CN/en）。
  - check.js/smoke.js：T-59 静态断言（通道/扫描/预览/错误分组/双语文案/安全边界未放宽）与运行时断言（临时 Codex pets 目录批量导入、失败分组、node_modules/隐藏目录跳过、defaultCodexPetsDir 优先 CODEX_HOME 并回退 HOME/.codex/pets、不存在的目录拒绝）；smoke 端到端（真实 IPC 导入 + 皮肤页 9 行预览 DOM + 失败分组面板渲染）。
- 验证结果：
  - npm run check 全绿（T-59 静态断言 + 目录扫描运行时断言通过）。
  - npm run smoke 全绿（T-59 目录扫描导入/失败分组/9 行预览端到端通过）。
  - 像素级目检说明：本机 `~/.codex/pets` 为空目录，无真实 Codex 宠物包可做像素验证；图集 9 行预览已做 DOM 级冒烟（9 行 cell 存在），真实观感待用户导入宠物包后人工目检。
- 边界说明：无越界触碰；index.html/store.js/contracts.js/主窗口相关文件零改动；新按钮与结果面板由 chat.js 动态注入（遵守 index.html 只读约束）。
