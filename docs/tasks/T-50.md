# T-50 设置页账户区并入分组 + 移除番茄钟

- 状态：已完成（已验收合并，2026-08-11）
- 分支：codex/m4-account-pomodoro
- 工作树：E:\codex\AI桌宠-m4-account-pomodoro（协调者已创建，基线 main ec57384）
- 目标：
  1. “账户与订阅”区块改为与其他设置分组一致的外观（行式分组：图标+组名+箭头，点击展开），不再单独占大卡片；保留全部既有元素 id 与功能。
  2. 整体移除番茄钟：聊天面板小部件、设置项（番茄钟提醒/时长）、渲染层逻辑、主进程通知轮询与系统通知、store 字段、telemetry 事件、license 权益、双语文案、check/smoke 断言同步清理。
- 涉及文件：
  - src/renderer/index.html
  - src/renderer/chat.js
  - src/renderer/chat.css
  - src/storage/store.js
  - src/main/ipc.js
  - src/main/main.js
  - src/main/license.js
  - src/main/telemetry.js
  - src/shared/locales/zh-CN.json、en.json
  - scripts/check.js
  - scripts/smoke.js
  - docs/tasks/T-50.md（本卡）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/shared/contracts.js、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡外）
- 账户区要求：
  - account-section 改为 settings-group 同款行式分组（图标建议 👤，组名“账户与订阅”，aria-expanded/aria-controls，与其他分组同 CSS 类）；展开面板保留：当前版本/状态/有效期/云额度/Pro 功能/沙箱支付/激活码/激活与注销/状态消息。
  - account-upgrade-btn“升级 / 激活”保留（可放面板内或行上），行为不变（点击滚动并聚焦激活码输入框）。
  - 全部既有 id 保留：account-section、account-upgrade-btn、license-tier/license-status/license-expiry/license-quota/license-pro-features/license-feature-*/license-code/license-activate/license-deactivate/license-message/payment-plans/payment-buy-yearly/payment-buy-lifetime/payment-message/payment-notice。
  - 分组顺序：账户与订阅为第一组（其余四组外观/对话/陪伴与效率/隐私与数据顺序不变）。
- 番茄钟移除要求（全链路清理）：
  - index.html：删除 pomodoro-widget 与设置页 pomodoro-enabled/pomodoro-minutes 字段；focus-widget 保留不动。
  - chat.js：删除 pomodoro 状态/定时器/事件监听/函数/apply/save/导出，以及 `track('pomodoro_complete'` 调用；focus 相关（recordFocusSession/renderFocusStats/getFocusStats/normalizeFocusStats）保留。
  - chat.css：删除番茄钟样式（#pomodoro-widget/.pomodoro-* 等）；专注/喝水/待办样式不动。
  - store.js：DEFAULT_SETTINGS 移除 pomodoroEnabled/pomodoroMinutes/pomodoroNotifyAt/pomodoroNotifyMinutes；清理 sanitize/patch/whitelist 与导出常量（DEFAULT_POMODORO_MINUTES、POMODORO_MINUTES_MIN/MAX）；focusStats 保留。
  - ipc.js：删除 consumePomodoroNotificationSignal（含其中 telemetry.track('pomodoro_complete')）及其导出；清理 DEFAULT_POMODORO_MINUTES import。
  - main.js：删除 POMODORO_POLL_MS/DEFAULT_POMODORO_MINUTES/pomodoroTimer/showPomodoroNotification/consumePomodoroNotificationRequest/start/stopPomodoroNotificationPolling 及调用与导出。
  - license.js：entitlements 移除 pomodoro；focusStats 保留。
  - telemetry.js：事件白名单与事件字段定义移除 pomodoro_complete。
  - locales（zh-CN/en）：删除 pomodoro.* 与 settings.pomodoroEnabled/pomodoroEnabledHint/pomodoroMinutes/pomodoroMinutesHint；focus.* 保留。
  - check.js：删除或改写全部番茄钟断言（含“番茄钟保留”、pomodoro-widget 存在、store 信号语义、telemetry pomodoro_complete、main 轮询存在、license entitlements.pomodoro、T-48 id 清单中的 pomodoro-enabled/pomodoro-minutes），可按“番茄钟已移除”的新语义断言；其余断言保持通过。
  - smoke.js：删除 pomodoro 通知信号消费幂等与渲染层倒计时端到端测试，保留其余测试。
- 验收标准：
  1. `npm run check` 全绿（含更新后的断言）
  2. `npm run smoke` 全绿
  3. 账户区为分组行外观，展开后可操作全部账户/订阅功能；既有 id 全部保留
  4. 界面与代码无番茄钟痕迹（面板/设置/通知/遥测/权益/文案）；focus-widget 保留
  5. 未触碰禁止文件；无外部副作用；不合并 main、不 push
- 完成定义：实现并自检（check/smoke），更新本卡状态为“已完成（待协调者验收）”，提交分支；回报（分支/提交/改动文件清单/验证结果/账户区最终结构）。
- 备注：worktree 已由协调者建好且 node_modules 已装；不要新建其他 worktree，不要改 main 工作区；专注统计将因失去番茄钟数据源而不再增长（本卡保留该组件，协调者会向用户说明）。

## 完成记录（2026-08-11，worker 填写）

- 状态：已完成（已验收合并）
- 分支：codex/m4-account-pomodoro（基线 main ff51bec；提交号见本次回报）
- 实现摘要：
  1. 账户与订阅：`settings-home` 中 `account-section` 由顶部大卡片改为与其他设置分组一致的行式分组（👤 + 组名“账户与订阅” + 展开箭头），作为第一组置于 `settings-groups` 内；其余四组（外观/对话/陪伴与效率/隐私与数据）顺序不变。组行复用 `settings-group-toggle`（可聚焦、aria-expanded/aria-controls，新增 `settings-group-toggle-account` / `settings-group-panel-account`）；展开面板保留当前版本/状态/有效期/云额度/Pro 功能/沙箱支付（yearly/lifetime）/激活码/激活/注销/状态消息；`account-upgrade-btn`“升级 / 激活”保留在面板内，点击行为不变（滚动并聚焦激活码输入框）；全部既有 account/license/payment id 保留。
  2. 番茄钟全链路移除：index.html 删除 pomodoro-widget 与设置页 pomodoro-enabled/pomodoro-minutes（focus-widget 保留不动）；chat.js 删除 pomodoro 状态/定时器/事件监听/函数与 settings 读写/导出（focus 相关函数保留）；chat.css 删除 #pomodoro-widget/.pomodoro-* 样式（专注/喝水/待办样式不动）；store.js 删除 pomodoroEnabled/pomodoroMinutes/pomodoroNotifyAt/pomodoroNotifyMinutes 默认值、清洗/白名单/patch 分支与导出常量，并对存量字段兼容清理；ipc.js 删除 consumePomodoroNotificationSignal（含 pomodoro_complete 遥测）及导出与 import；main.js 删除 POMODORO_POLL_MS/pomodoroTimer/系统通知/信号轮询及启动/退出调用/导出；license.js entitlements 移除 pomodoro（focusStats 保留）；telemetry.js 移除 pomodoro_complete 事件白名单与字段规则；zh-CN/en 删除 pomodoro.* 与 settings.pomodoro*（含合规提示文案中的番茄钟字样）；check.js/smoke.js 断言按“番茄钟已移除”新语义改写并删除端到端测试。
- 改动文件：src/renderer/index.html、src/renderer/chat.js、src/renderer/chat.css、src/storage/store.js、src/main/ipc.js、src/main/main.js、src/main/license.js、src/main/telemetry.js、src/shared/locales/zh-CN.json、src/shared/locales/en.json、scripts/check.js、scripts/smoke.js、docs/tasks/T-50.md（本卡）
- 验证结果：
  - `npm run check`：全部通过（含“番茄钟已移除”断言、账户分组结构与既有 id 断言、存量 pomodoro 字段兼容清理断言）
  - `npm run smoke`：全部通过（渲染页加载、启动历史恢复、chat 表单端到端；番茄钟通知信号/倒计时测试已删除）
- 遗留风险：
  - 专注统计（focusStats）组件保留，但其唯一数据源（番茄钟完成）已移除，统计数据不再增长；如需恢复增长需另接数据源或移除组件（ADR-038 已记录）。
  - 存量用户 settings.json 中的 pomodoro 字段在下一次读/写时被删除，不迁移、不兼容读取（ADR-038 预期行为）。
- 未做 `npm run dev` 人工目检（本卡为无头执行）；账户分组展开/收起复用既有 `bindSettingsGroups` 逻辑，check 已断言结构与接线。

## 验收合并记录（2026-08-11，协调者）

- 复核：改动 13 个文件均在任务卡允许范围；未触碰 package.json、contracts.js、locales/index.js 等禁止文件；分支基于 ff51bec，fast-forward 合并 714b706。
- 结构复核：account-section 已改为 settings-groups 第一组（settings-group-toggle-account/panel-account，👤 + 账户与订阅 + 箭头，aria 完整），account-upgrade-btn 与 license/payment 全部 id 保留；番茄钟全链路无残留（面板/设置/store 字段/主进程轮询/系统通知/telemetry 事件/license 权益/文案/断言均已移除；残留引用仅为“已移除”断言与历史注释）。
- 兼容性：store 对存量 pomodoro 字段做删除清理（不迁移/不暴露）；focusStats 保留（数据源移除后不再增长，ADR-038 已记录）。
- 验证：worker 与协调者 `npm run check`、`npm run smoke` 全绿（check 含“番茄钟已移除”新语义断言）。
- 遗留：chat.js 两处历史注释仍提及番茄钟（无功能影响）；专注统计无数据源；视觉效果用户已目检确认（2026-08-11 14:17，效果可以）。
