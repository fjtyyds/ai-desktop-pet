# T-30 系统状态小部件移除

- 状态：已完成（待协调者验收）
- 分支：codex/m3x-status-remove
- 工作树：E:\codex\AI桌宠-m3x-status-remove
- 目标：系统状态小部件（CPU/内存/电池）与“陪伴”定位不符，移除小部件及相关轮询
- 涉及文件：
  - src/main/main.js（readSystemStatus/broadcastSystemStatus/startSystemStatusWidgets/statusTimer/batteryTimer、idle 事件中 system-status 分支）
  - src/renderer/index.html（widgetsPanel 标记）
  - src/renderer/chat.js（applySystemStatus、toggleWidgets、相关状态）
  - src/renderer/chat.css（widgets 样式清理）
  - src/storage/store.js（widgetsEnabled 字段清理或兼容）
  - src/shared/locales/zh-CN.json、en.json（widgets.* 文案清理）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：无
- 验收标准：
  1. UI 不再显示系统状态小部件及设置入口
  2. 主进程不再轮询 CPU/内存/电池状态
  3. 空闲主动话术（idle 聊天互动）仍正常，不被误删
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。

## 完成记录（2026-08-10）

- 分支：codex/m3x-status-remove；代码提交：c209d5d；任务卡提交：见本分支 HEAD。
- 改动文件：
  - src/main/main.js：移除 readSystemStatus/broadcastSystemStatus/startSystemStatusWidgets/stopSystemStatusWidgets/statusTimer/batteryTimer 及 CPU/内存/电池实现与相关导入（os/execFile/powerMonitor）；idle:event 不再推送 system-status；新增 startPomodoroNotificationPolling（2s 独立轮询）替代原 broadcastSystemStatus 中的番茄钟信号消费，保证番茄钟系统通知不回归。
  - src/renderer/index.html：移除 widgetsPanel/system-status-widget/widgets-toggle/widget-stats/stat-* 标记与设置页系统状态小部件开关；保留番茄钟小部件。
  - src/renderer/chat.js：移除 applySystemStatus/toggleWidgets/syncWidgetsVisibility/lastSystemStatus 及相关元素引用；idle 订阅仅保留空闲话术分支；设置读写不再包含 widgetsEnabled。
  - src/renderer/chat.css：移除 widgets-panel/widgets-close-btn/widget-stats/stat-* 样式；番茄钟卡片单独保留布局边距。
  - src/storage/store.js：移除 widgetsEnabled 默认值/清洗/白名单（旧 settings.json 残留字段兼容忽略）。
  - src/shared/locales/zh-CN.json、en.json：移除 widgets.* 与 settings.widgetsSection/widgetsEnabled/widgetsEnabledHint 文案；idle 话术保留。
  - scripts/check.js：新增 T-30 移除回归断言（主进程/UI/设置/文案/存储五触点 + idle 与番茄钟保留断言）。
- 自检：`npm run check` 通过；`npm run smoke` 通过（真实用户权限）。
- UI 目检：无头 Electron 加载渲染页，DOM 断言 widgets-panel/system-status-widget/widgets-toggle/widgets-enabled/stat-* 均不存在，pomodoro-widget 可见、chat/weather 正常；截图保存于 C:\Users\HP\.codex\visualizations\2026\08\10\019fe969-f00f-7863-ad9a-7ff47c79b98d\t30-ui.png。像素级人工目检待协调者/用户复核。
- 边界说明：未触碰 package.json/package-lock.json/contracts.js/API.md/PLAN/STATUS/AGENTS/DECISIONS/scripts/orchestrator；未修改 preload.js/ipc.js。番茄钟通知消费原挂在 broadcastSystemStatus 上，为避免功能回归，在 main.js 内迁移为独立 2s 轮询；该机制与 T-27（番茄钟重复弹窗修复）职责重叠，建议由协调者验收时与 T-27 协调统一。
