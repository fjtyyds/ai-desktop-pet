# T-01 托盘与窗口生命周期

- 状态：已完成
- 分支：codex/m1-tray
- 工作树：E:\codex\AI桌宠-m1-tray
- 目标：托盘图标、显示/隐藏、退出、单实例锁
- 涉及文件：
  - src/main/main.js（重构窗口生命周期）
  - src/main/tray.js（新建，托盘与菜单）
- 禁止触碰：src/renderer/**、src/llm/**、src/storage/**、src/shared/**、package.json
- 接口依赖：无（窗口拖拽区已在 M0 完成）
- 验收标准：
  1. `npm run check` 通过
  2. `npm run smoke` 通过
  3. 手动 `npm run dev`：出现托盘图标；右键菜单可显示/隐藏、退出；再次启动不产生第二个实例
- 完成定义：全部验收通过并提交到 codex/m1-tray

## 完成记录（2026-08-09）

- 实现：托盘图标（内嵌 PNG 图标）、右键菜单（显示/隐藏、退出）、左键单击切换窗口、关闭窗口隐藏到托盘、单实例锁。
- 验证：`npm run check` 通过；`npm run smoke` 通过；自动启动验证通过（首次实例正常运行、二次启动立即退出且无报错）。
- 待人工目检：`npm run dev` 下托盘图标右键菜单的显示/隐藏与退出交互。
