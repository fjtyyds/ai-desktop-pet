# T-57 状态机与气泡队列：speaking/attention + 队列 + 提醒透出

- 状态：待派发（2026-08-13）
- 分支：codex/m5x-status；工作树：E:\codex\AI桌宠-m5x-status（总工创建，node_modules 为 junction）
- 目标：浮窗状态从 4 态扩展为 6 态（+speaking/attention），气泡支持队列轮播与时长配置，空闲互动/喝水提醒可透出到浮窗（主窗口隐藏时也可见）。
- 涉及文件（只允许改这些）：
  - src/main/pet-overlay.js
  - src/main/main.js
  - src/main/preload.js
  - src/renderer/overlay.js
  - src/renderer/overlay.css
  - src/renderer/chat.js
  - src/shared/locales/zh-CN.json、en.json（overlay.statusSpeaking/statusAttention 等）
  - scripts/check.js、scripts/smoke.js
  - docs/tasks/T-57.md（本卡）
- 只读文件：src/storage/store.js（ADR-045 已预冻结 petOverlayBubbleSeconds/BubbleEnabled/Reminders）、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、src/renderer/index.html、src/renderer/chat.css、src/main/ipc.js、src/main/skin-store.js
- 实现要求：
  1. 状态扩展：`speaking`（TTS 朗读中）、`attention`（提醒/空闲互动）；VALID_STATES 同步扩展；气泡文案双语文案。
  2. 气泡队列：主进程维护最多 3 条待显示状态（含 text），浮窗按 petOverlayBubbleSeconds（默认 6s，3~20）逐条轮播；ready/failed 仍自动回落 idle。
  3. 设置生效：petOverlayBubbleEnabled=false 时浮窗不显示气泡（宠物动画照常）；petOverlayReminders 控制空闲互动/喝水等提醒是否透出。
  4. 空闲互动透出：主窗口隐藏时，idleMonitor 触发的事件同时推送给浮窗（pet:push-bubble 语义，attention + 互动文案）。
  5. TTS 朗读：chat.js 在 speak 开始/结束调用 petAPI.petOverlay.setStatus({state:'speaking'}) / 恢复 idle（失败也恢复）。
  6. 新增 IPC `pet:push-bubble`（总工已冻结在 contracts.js：pushBubble({state,text})）；preload 暴露。
- 验收标准：
  1. npm run check 全绿（新增 T-57 断言：状态集合/队列/开关接线）。
  2. npm run smoke 全绿（含 pushBubble 队列端到端）。
  3. 设置字段只读使用，不修改 store.js。
- 完成定义：实现并自检 → 更新本卡状态 → 提交分支 → 回报。
