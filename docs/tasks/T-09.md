# T-09 设置页 API Key 与模型输入（常开真实模式）

- 状态：未开始
- 分支：codex/m2-apikey
- 工作树：E:\codex\AI桌宠-m2-apikey
- 目标：设置页支持配置 API Key 与模型，保存后无需环境变量即可使用真实 DeepSeek
- 涉及文件：
  - src/renderer/index.html（设置页新增 API Key 密码框、模型输入与保存状态提示）
  - src/renderer/chat.js（settings.get 预填、settings.set 保存 apiKey/model；保存后提示“密钥仅保存在本机”）
  - src/renderer/chat.css（如需 password 输入与说明文字样式，优先复用 .field）
  - src/storage/store.js（apiKey/model 清洗上限：apiKey ≤ 256、model ≤ 100，超长截断/非法丢弃）
  - scripts/check.js（可选：apiKey/model 读写与清洗断言）
- 只读文件：src/shared/contracts.js（AppSettings.apiKey/model 已冻结）；src/main/ipc.js、src/main/preload.js（settings.get/set 已存在，只调用）
- 禁止触碰：package.json、src/llm/**、src/storage/memory-store.js、src/main/main.js
- 依赖：ADR-015（触点清单已确认）
- 验收标准：
  1. `npm run check` 通过；`npm run smoke` 通过
  2. 设置页可填写/保存 API Key（密码框）与模型，重启后仍可读取
  3. 保存 API Key 后无需环境变量即可真实对话（用 `deepseek-v4-flash` 或设置中的模型真实调用一次）
  4. 非法/超长输入被清洗，settings.json 不被破坏；UI 说明“密钥仅保存在本机”
- 完成定义：全部验收通过并提交到 codex/m2-apikey

## 设计依据

- ADR-015（设置页补 API Key 与模型输入）；SPEC MVP 用户故事 3
