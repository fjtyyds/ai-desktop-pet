# T-38 发布产物命名 ASCII 化（修复 latest.yml 与实际产物不一致）

- 状态：已完成（已验收合并，2026-08-10）
- 分支：codex/m4-artifact-name（基线 main a34593f）
- 工作树：E:\codex\AI桌宠-m4-artifact-name
- 目标：electron-builder 的 artifactName 当前为 `AI桌宠-${version}-Setup.${ext}`（含非 ASCII），生成的 latest.yml 引用安全化文件名（如 ai-desktop-pet-setup-0.1.0.exe）而实际产物为中文名，electron-updater 更新源将 404；改为纯 ASCII 命名，使 latest.yml 引用与实际产物一致。
- 涉及文件：
  - electron-builder.yml（仅 artifactName 行；本卡由协调者预授权修改，其余配置不动）
  - scripts/check.js（新增断言：artifactName 纯 ASCII、产物命名约定、latest.yml 引用与产物一致性的静态检查）
  - docs/tasks/T-38.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md、.github/workflows/ci.yml
- 禁止触碰：package.json、package-lock.json、src/**、docs/**（除本卡外）、scripts/orchestrator/**
- 依赖：T-37 已合并（4ec503a）；publish 配置已就绪

## 验收标准

1. artifactName 使用纯 ASCII（建议 `ai-desktop-pet-${version}-Setup.${ext}`），productName 保持“AI桌宠”不变（安装/卸载/快捷方式显示名不受影响）
2. 本地 `npm run dist -- --publish never` 实际产物为 ASCII 文件名，dist/latest.yml（或生成的更新清单）引用的文件名与实际产物完全一致
3. `npm run check` 通过（含 ASCII artifactName 断言）；`npm run smoke` 通过或说明原因
4. 不触碰 package.json、package-lock.json、src/**、CI 工作流
5. 未发布真实版本时，写明验证策略（本地打包 + latest.yml 文件名比对），不得伪造通过

## 完成定义

实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不自行 rebase；完成后回报协调者。

## 协调者验收（2026-08-10）

- 3315146 fast-forward 合并入 main；main check/smoke 通过；worktree 已清理，分支保留。

## 完成记录（2026-08-10）

- 改动文件（仅允许范围内的 3 个）：
  - `electron-builder.yml`：仅改 `nsis.artifactName`，`AI桌宠-${version}-Setup.${ext}` → `ai-desktop-pet-${version}-Setup.${ext}`；`productName: AI桌宠` 保持不变
  - `scripts/check.js`：新增 T-38 断言（artifactName 纯 ASCII 且为约定命名、productName 保持 AI桌宠、dist 存在时产物文件名 ASCII 与 latest.yml 引用静态比对）
  - `docs/tasks/T-38.md`：本卡状态与完成记录
- 验证结果：
  - `npm run check`：通过（含 `T-38 artifactName 纯 ASCII 且 productName 保持 AI桌宠`）
  - `npm run smoke`：通过（渲染页加载/历史恢复/番茄钟幂等/chat 端到端）
  - `npm run dist -- --publish never`：成功，产物 `dist/ai-desktop-pet-0.1.0-Setup.exe`（100,568,284 B）与 `.blockmap` 均为 ASCII 文件名
  - `dist/latest.yml` 引用 `ai-desktop-pet-0.1.0-Setup.exe`，与实际产物完全一致；打包后重跑 `npm run check` 的静态比对断言通过
- 边界合规：未触碰 package.json、package-lock.json、src/**、.github/workflows/ci.yml、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、docs/API.md、AGENTS.md、scripts/orchestrator/**
- 遗留风险：真实更新链路需首次 v 标签发布验证（push/tag 属远程操作，由协调者/用户确认后执行）；CI 产物名将随本改动变为 ASCII 文件名
