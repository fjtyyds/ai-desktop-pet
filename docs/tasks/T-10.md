# T-10 打包与正式图标

- 状态：已完成
- 分支：codex/m3-packaging
- 工作树：E:\codex\AI桌宠-m3-packaging
- 目标：electron-builder + NSIS 产出 Windows 安装包；正式图标（应用/托盘/安装包）
- 涉及文件：
  - package.json（新增 electron-builder devDependency 与 build 配置；依赖变更需协调者批准后执行）
  - electron-builder.yml（新建，NSIS 配置）
  - assets/icon.ico、assets/icon.png（新建正式图标；托盘图标改为读取 assets，保留内嵌 base64 开发回退）
  - .gitignore（忽略 dist/ 输出）
  - 打包后安装/解包启动冒烟验证记录
- 只读文件：src/** 业务代码、scripts/check.js（协调者维护）
- 禁止触碰：src/renderer/**、src/main/**（图标读取除外，如需改 tray.js 需记录到本卡）、src/llm/**、src/storage/**
- 依赖：ADR-016
- 验收标准：
  1. `npm run check`、`npm run smoke` 通过
  2. `npm run dist` 产出 Windows NSIS 安装包（dist/ 下）
  3. 安装或解包后应用可启动，托盘/聊天/设置可用；应用图标与托盘图标为正式图标
  4. 打包产物启动冒烟（应用进程可运行，单实例锁正常）
- 完成定义：全部验收通过并提交到 codex/m3-packaging

## 设计依据

- ADR-016（electron-builder + NSIS + assets 正式图标）

## 完成记录（2026-08-09）

- 依赖：新增 devDependency `electron-builder@^26.15.3`（协调者批准后安装）；配置集中在 `electron-builder.yml`（appId `com.aideskpet.app`、productName `AI桌宠`、win x64 + NSIS、oneClick=false、允许改安装目录、桌面/开始菜单快捷方式、安装/卸载图标均用 `assets/icon.ico`）；`package.json` 增加 `dist`/`pack` 脚本与 author 元数据。
- 图标：`assets/icon.ico`（16/24/32/48/64/128/256 多尺寸）与 `assets/icon.png`（512x512，透明圆形蓝底白爪印，延续原占位图身份）；`src/main/tray.js` 与 `src/main/main.js` 改为读取 `assets/icon.png`，失败回退内嵌 base64；`.gitignore` 已含 `dist/`，无需改动。
- 集成：执行 `git merge main`，解决 main.js（同时保留 initCrash 与 loadAppIcon）与 tray.js（同时保留 i18n 本地化与 assets 图标）冲突；未改 PLAN/STATUS，未合并 main。
- 校验：`npm run check`、`npm run smoke` 通过；`npm run dist` 产出 `dist/AI桌宠-0.1.0-Setup.exe`（约 100 MB）与 `dist/win-unpacked/`。
- 解包冒烟（`--user-data-dir` 隔离）：应用进程存活；CDP 验证 `petAPI` 存在、`settings.get` 正常、初始历史为 0、`chat.send` mock 回复正常、`settings.set/get` 密钥往返一致；临时 profile 的 `settings.json` 中 apiKey 以密文落盘（非 sk- 明文，T-13 生效）；`logs/app.log` 记录 crashReporter 启动；第二实例立即退出且第一实例存活（单实例锁）。
- 安装冒烟：`/S` 静默安装到临时目录（安装器退出码 0），启动已安装 `AI桌宠.exe` 后上述 CDP 断言全部通过；静默卸载退出码 0。
- 备注：首次冒烟尝试用 `APPDATA` 环境变量隔离失败（Electron 实际仍读真实 userData），已改用 `--user-data-dir`；误写入真实历史的 2 条测试消息已清理（恢复为 24 条，备份 `%TEMP%\ai-pet-messages-backup-16b81fb4396b4178920aedffe64ef103.json`）；真实 `settings.json` 的 apiKey 已被 T-13 自动迁移为密文（预期行为）。
