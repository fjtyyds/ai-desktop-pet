# T-56 浮窗交互增强：点击唤起主窗口/右键菜单/Esc 收起/贴边吸附

- 状态：待派发（2026-08-13）
- 分支：codex/m5x-interact；工作树：E:\codex\AI桌宠-m5x-interact（总工创建，node_modules 为 junction）
- 目标：把浮窗从“只能看”变成“可交互”：点击宠物唤起/隐藏主窗口，右键菜单，Esc 收起，拖动贴边吸附。
- 涉及文件（只允许改这些）：
  - src/main/pet-overlay.js
  - src/main/main.js
  - src/main/preload.js
  - src/renderer/overlay.html
  - src/renderer/overlay.css
  - src/renderer/overlay.js
  - src/shared/locales/zh-CN.json、en.json（新增 overlay.* 文案）
  - scripts/check.js、scripts/smoke.js
  - docs/tasks/T-56.md（本卡）
- 只读文件：src/storage/store.js、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、scripts/orchestrator/**、src/renderer/chat.js、src/renderer/index.html、src/renderer/chat.css、src/main/ipc.js
- 实现要求：
  1. 单击/双击宠物区域唤起主聊天窗口（第一次显示并聚焦；主窗口可见时再点则隐藏到托盘）；主进程注入 showMainWindow 回调，新增 IPC `pet:show-main`（preload 暴露 petAPI.petOverlay.showMain）。
  2. 浮窗右键菜单（contextmenu）：显示主窗口 / 收起宠物 / 切换气泡显示（本地记忆可先走 sessionStorage）/ 退出应用；菜单文案双语文案。
  3. 浮窗获得焦点时按 Esc 收起（调用 tuckAway 语义：隐藏并持久化 petOverlayEnabled=false）。
  4. 拖动贴边吸附：简化版——拖动结束后距屏幕边缘 ≤24px 则对齐该边缘（复用 DOCK_MARGIN 思路，但浮窗内独立实现，不侵入 main.js 主窗口 T-31 逻辑）；位置继续记忆（含 displayId）。
  5. 无障碍：右键菜单项、Esc 说明进入 aria-label/帮助文案。
- 验收标准：
  1. npm run check 全绿（新增 T-56 断言：showMain 通道、右键菜单、Esc 处理存在）。
  2. npm run smoke 全绿或说明原因（含 showMain 端到端可选）。
  3. 离屏/像素或真窗口验证：浮窗可显示、右键菜单元素存在。
  4. 未触碰禁止文件；契约文件只读。
- 完成定义：实现并自检（check/smoke）→ 更新本卡状态为“已完成（待总工验收）” → 提交分支 → 回报（分支/提交/改动清单/check/smoke/UI 目检结论/边界说明）。
