# T-36 TTS 听感优化：96kbps 输出 + 语调参数重调

- 状态：未开始（2026-08-10 用户反馈：语音包听感僵硬、像 AI 合成）
- 分支：codex/m3x-tts-prosody（建议）
- 工作树：E:\codex\AI桌宠-m3x-tts-prosody（建议）
- 背景与实测结论（协调者已完成）：
  - 运行中应用确认走 Edge 神经语音（非系统回退），问题在朗读腔与音质
  - 免费端点不支持 `mstts:express-as` 与 `<break>`（实测均返回无音频），本次不做此类尝试
  - 输出格式 `audio-24khz-96kbitrate-mono-mp3` 可用（当前 48kbps 压缩感明显）；48kHz 格式被拒
  - 当前语调参数偏极端（playful +10%/+8Hz、cool -5%/-4Hz、curious +5%/+4Hz），听感不自然
- 目标：
  1. `src/main/tts-edge.js` 输出格式改为 `audio-24khz-96kbitrate-mono-mp3`
  2. `src/renderer/chat.js` 的 `TTS_VOICE_PACKS` 按新参数表调整 `edgeRate`/`edgePitch`（voice 不变）
  3. `scripts/check.js` 同步断言（96kbps 格式常量 + 新参数表）
- 涉及文件：
  - src/main/tts-edge.js
  - src/renderer/chat.js
  - scripts/check.js
  - docs/tasks/T-36.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md（契约不变）、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、scripts/orchestrator/**、src/** 之外文件（本卡除外）

## 新参数表（协调者已生成 v2 演示音频验证，全部成功）

| id | voice | edgeRate | edgePitch |
| --- | --- | --- | --- |
| warm | zh-CN-XiaoxiaoNeural | -3% | +1Hz |
| sage | zh-CN-YunyangNeural | -5% | -1Hz |
| playful | zh-CN-YunxiNeural | +6% | +3Hz |
| gentle | zh-CN-XiaoyiNeural | -6% | +0Hz |
| cool | zh-CN-YunjianNeural | -2% | -2Hz |
| curious | zh-CN-YunxiaNeural | +2% | +2Hz |

## 验收标准

1. 合成音频字节率约 12KB/s（96kbps）；6 套参数与新参数表逐项一致
2. LRU 缓存、10s/20s 超时、失败回退 speechSynthesis 行为不回归
3. `npm run check` 通过；`npm run smoke` 通过或说明原因
4. 听感较 T-34 v1 明显自然（主观项，协调者与用户试听复核）

## 完成定义

实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不自行 rebase；完成后回报协调者。
