# T-48 设置界面布局重构（豆包式分组列表）

- 状态：已派发（2026-08-11）
- 分支：codex/m4-settings-layout
- 工作树：E:\codex\AI桌宠-m4-settings-layout（协调者已创建，基线 main 2938cb0）
- 目标：参考用户提供的两张豆包设置页截图（OCR 描述见下），把设置页从“单页堆叠表单”重构为“顶部账号卡片 + 分组列表 + 版本页脚”，保留全部元素 id 与既有功能。
- 参考图（新线程同样无法直接查看图片，以下为前任总工 Windows OCR 提取）：
  - `C:\Users\HP\OneDrive\桌面\e84576ba0b9f88e1e3322918d9c10c34.jpg`（上部）：顶部账号卡片（用户 438422 / 豆包号 / 豆包账号管理 / 豆包专业版 / 豆包形象 / 立即升级 / 宝宝冯 1.0 倍 / 已开启），下方分组列表：字号与背景、记忆、开启新话题、查找聊天内容、新对话默认模式。
  - `C:\Users\HP\OneDrive\桌面\dc5297d8a52f1949976ee2bf5da0415b.jpg`（下部）：未成年人模式、云盘存储、钱包、我的订单、隐私与权限、帮助与反馈、豆包官网、关于豆包、我的智能设备、切换账号、退出登录、Version 14.0.0、由豆包大模型提供支持、《个人信息清单》《三方信息共享清单》。
  - 推断目标布局：顶部账号/订阅卡片（含升级按钮）+ 分组列表行（图标+文字+箭头）+ 底部版本与支持信息页脚（ADR-036）。
- 涉及文件：
  - src/renderer/index.html（settings-view/settings-home 结构重构；保留全部既有元素 id）
  - src/renderer/chat.css（账号卡片、分组列表、展开动画、页脚样式；与 T-44 玻璃拟态风格一致）
  - src/renderer/chat.js（分组展开/收起、跳转行、页脚版本号渲染；保留全部既有事件绑定与 id 引用）
  - src/shared/locales/zh-CN.json、en.json（新增分组标题/页脚/展开提示等文案 key）
  - scripts/check.js（新增 T-48 断言：分组容器存在、全部既有 settings 元素 id 仍存在、页脚版本号非空；如无需改动请在完成记录说明理由）
  - docs/tasks/T-48.md（本卡状态与完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/main/**、src/storage/**、src/llm/**、src/shared/contracts.js、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡外）
- 依赖：无
- 分组建议（ADR-036，最终命名可微调）：
  1. 外观：主题、界面语言、减弱动效、皮肤与配件（跳转行）
  2. 对话：API Key、模型、宠物名称、人格模板（含性格标签/语气/背景）、专属语音包、语音包
  3. 陪伴与效率：空闲互动、天气小部件、城市、喝水提醒、提醒间隔、番茄钟提醒、番茄钟时长
  4. 隐私与数据：匿名遥测（含关闭并清除）、记忆管理（跳转行）、清除数据
  - 顶部账号卡片：复用 account-section 全部元素（当前版本/状态/有效期/云额度/Pro 功能/激活码/沙箱购买），改为卡片视觉 + 显眼“升级/激活”入口。
  - 底部页脚：应用版本号（可用 window.petAPI.version）+“由 DeepSeek 大模型提供支持”等静态支持信息；不恢复平台/系统信息（T-24 旧决策保留）。
  - 保存按钮（settings-save）保持可用，建议固定可见；保存行为与现状一致。
- 验收标准：
  1. 顶部账号卡片 + 分组列表 + 版本页脚三段式呈现，视觉对齐 OCR 描述（卡片含升级入口；分组行为图标+文字+箭头；页脚含版本号）
  2. 全部既有元素 id 与功能保留：api-key/model/language/theme/reduce-motion/idle-enabled/weather-enabled/weather-city/water-enabled/water-interval/pomodoro-enabled/pomodoro-minutes/telemetry-enabled/telemetry-clear/pet-name/persona-template-list/persona-traits/persona-tone/persona-backstory/tts-voice-pack-enabled/tts-voice-pack-id/memory-manage-btn/skin-manage-btn/settings-save/settings-status/clear-scope/clear-data/clear-status/license-*/payment-*/account-section 等；settings-home 打开后所有控件可读写
  3. memory-page、skin-page、compliance-view 子页与跳转不受影响
  4. i18n：新增文案在 zh-CN/en 均有 key；切换语言无缺 key 报错
  5. 无障碍：分组行可聚焦、展开状态表达（aria-expanded）、对比度不劣于现状
  6. 未触碰禁止文件；`npm run check` 通过；`npm run smoke` 通过
- 完成定义：实现并自检（check/smoke），更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push；回报协调者（分支/提交/改动文件清单/验证结果/最终分组与命名）。
- 备注：worktree 若 node_modules 缺失，可先 `npm install`（不改 package.json；慢时可使用 ELECTRON_MIRROR 镜像，见 AGENTS.md）；不要新建其他 worktree，不要改 main 工作区。

## 完成记录（2026-08-11，待 worker 填写）
