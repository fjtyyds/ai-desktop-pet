# T-39 M4 P4 最终打包与安装冒烟

- 状态：已完成（待协调者验收）（2026-08-10，M4 P4）
- 分支：codex/m4-final-dist（建议）
- 工作树：E:\codex\AI桌宠-m4-final-dist（建议）
- 目标：在最终 main（含 T-37 自动更新、T-38 ASCII 产物命名）上执行 `npm run dist -- --publish never` 产出正式安装包，并对产物做安装/卸载冒烟；确认 win-unpacked 内 app-update.yml 指向 GitHub Releases（为首次发布做准备）。
- 涉及文件：
  - docs/tasks/T-39.md（本卡完成记录）
  - scripts/check.js（可选：增加最终产物断言，如 app-update.yml 存在性与 provider 指向）
  - dist/ 产物为本地验证产物，不入库
- 只读文件：package.json、package-lock.json、electron-builder.yml、.github/workflows/ci.yml、src/**、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、docs/API.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、src/**、.github/workflows/**、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、docs/API.md、AGENTS.md、scripts/orchestrator/**；禁止 push、禁止创建 GitHub Release、禁止合并 main
- 依赖：T-37/T-38 已合并（main 19e9daf）；首次真实发布（v 标签 + Release）属远程操作，由用户确认后另行执行

## 验收标准

1. 基线为 main 最新（19e9daf）；`npm run check`、`npm run smoke` 通过
2. `npm run dist -- --publish never` 成功（退出码 0），产物为 ASCII 文件名（ai-desktop-pet-0.1.0-Setup.exe 及 blockmap）
3. dist/latest.yml 引用与实际产物完全一致（check 静态比对通过）
4. dist/win-unpacked/resources/app-update.yml（或等效更新清单）存在，provider/owner/repo 指向 github / fjtyyds / ai-desktop-pet
5. 安装冒烟：静默安装/卸载退出码 0，使用临时/独立 userData，不污染真实用户数据；受环境限制时明确说明原因并给替代证据（如解包启动冒烟）
6. 边界合规：未触碰禁止文件；dist/、日志不提交

## 完成定义

实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push、不创建 Release；完成后回报协调者。

## 完成记录（2026-08-10）

- 分支：codex/m4-final-dist（未合并 main、未 push、未创建 Release）。
- 基线说明：任务描述写“main 最新 19e9daf”，实际 main 最新为 2ebb122（19e9daf 之后仅新增本任务卡提交）；分支基于 main HEAD 2ebb122，包含 19e9daf 全部内容。
- 改动文件：
  - `docs/tasks/T-39.md`（本卡完成记录）
  - `scripts/check.js`（可选追加已实施：T-39 断言——dist/win-unpacked/resources/app-update.yml 存在且 provider/owner/repo = github/fjtyyds/ai-desktop-pet）
- 验证结果：
  1. `npm run check` 通过（含 T-38 dist/latest.yml 静态比对：1 个引用与实际产物一致；T-39 app-update.yml 断言通过）。
  2. `npm run smoke` 通过（渲染页加载、历史恢复、番茄钟幂等、chat 端到端）。
  3. `npm run dist -- --publish never` 退出码 0；顶层产物 `ai-desktop-pet-0.1.0-Setup.exe`（100,568,325 B）与 `ai-desktop-pet-0.1.0-Setup.exe.blockmap`（105,706 B），均为 ASCII 文件名。
  4. `dist/latest.yml` 的 url/path 均为 `ai-desktop-pet-0.1.0-Setup.exe`，与实际产物完全一致。
  5. `dist/win-unpacked/resources/app-update.yml` 存在：provider=github、owner=fjtyyds、repo=ai-desktop-pet。
  6. 安装冒烟：静默安装（`/S /D=临时目录`）退出码 0；以 `--user-data-dir=临时目录` 启动安装版，进程存活 10s，应用数据（Chromium 缓存、logs/app.log、updater 缓存等）全部写入临时目录；真实 `%APPDATA%\AI桌宠` 自始至终未被创建（零污染）；静默卸载（`/S`）退出码 0，安装目录完全移除。
  7. 更新链路证据：安装版启动后 updater 按预期报“No published versions on GitHub”（当前无 v0.1.0 Release），证明 app-update.yml 已生效并指向 GitHub；真实下载/安装链路待首次发布验证。
- 边界合规：仅修改 `scripts/check.js` 与 `docs/tasks/T-39.md`；dist/、打包日志、临时冒烟目录均未提交（临时目录已清理）；未触碰 package.json、package-lock.json、electron-builder.yml、src/**、.github/workflows/**、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、docs/API.md、AGENTS.md、scripts/orchestrator/**。
- 遗留风险：真实更新链路（electron-updater 从 GitHub Releases 下载并安装）需首次 v0.1.0 发布（v 标签 + Release）后才能端到端验证；正式代码签名/商店渠道待 M4 P2；安装包无代码签名证书时 Windows SmartScreen 可能提示未知发布者。
