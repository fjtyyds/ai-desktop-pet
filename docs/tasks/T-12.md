# T-12 i18n 与无障碍

- 状态：未开始
- 分支：codex/m3-i18n
- 工作树：E:\codex\AI桌宠-m3-i18n
- 目标：文案抽取 zh-CN/en；无障碍走查（ARIA/焦点/对比度）
- 涉及文件：
  - src/shared/locales/zh-CN.json、en.json（新建）
  - src/shared/locales/index.js（新建：locale 读取与翻译函数）
  - src/renderer/chat.js、src/renderer/index.html（文案替换 + 语言切换入口）
  - src/renderer/chat.css、styles.css（对比度与焦点样式走查修正）
  - src/main/tray.js（托盘菜单文案走 locale）
  - src/storage/store.js（如需 settings.language 字段与默认值；属本任务触点，需记录）
- 只读文件：scripts/check.js（协调者维护）、src/shared/contracts.js（如需语言契约，先提交提案给协调者）、src/llm/**
- 禁止触碰：package.json、src/llm/**
- 依赖：ADR-018
- 验收标准：
  1. `npm run check`、`npm run smoke` 通过
  2. 核心界面与托盘菜单文案不再硬编码中文；zh-CN/en 可切换（默认跟随系统，设置页可选语言并保存）
  3. 无障碍走查：主要按钮有 aria-label/标题、Tab 焦点可见、对比度无阻断项（记录走查结果）
- 完成定义：全部验收通过并提交到 codex/m3-i18n

## 设计依据

- ADR-018（i18n 与无障碍）
