# T-03 DeepSeek Provider、设置与本地存储

- 状态：已完成
- 分支：codex/m1-llm
- 工作树：E:\codex\AI桌宠-m1-llm
- 目标：实现 src/shared/contracts.js 定义的 petAPI 能力：chat.send、settings.get/set；DeepSeek 调用（无 API Key 时使用 mock 模式）；消息本地持久化
- 涉及文件：
  - src/llm/**（新建，provider 与 mock）
  - src/storage/**（新建，本地存储）
  - src/main/preload.js（暴露 petAPI）
  - src/main/ipc.js（新建，IPC handler）
  - package.json（T-03 是 M1 依赖变更的唯一负责人；如需要新增依赖须在任务卡记录）
- 只读文件：src/shared/contracts.js（如需修改契约，先提交提案给协调者）
- 禁止触碰：src/renderer/**、src/main/main.js（如需联动托盘/窗口，记录到任务卡并知会协调者）
- 验收标准：
  1. `npm run check` 通过
  2. `npm run smoke` 通过
  3. mock 模式下 chat.send 返回回复；settings 可保存/读取；消息重启后仍在
  4. 真实调用可用环境变量 DEEPSEEK_API_KEY 测试，密钥不硬编码
- 完成定义：全部验收通过并提交到 codex/m1-llm

## 完成记录（2026-08-09）

- 实现：提交 842f18a（codex/m1-llm）——src/llm/provider.js、mock.js、chat.js、index.js；src/storage/store.js、index.js；src/main/ipc.js；src/main/preload.js（petAPI.chat.send / settings.get / settings.set）。package.json 未变更。
- 集成：main.js 增加 `require('./ipc')` 完成接线；mock 模式返回回复、设置可读写、消息可持久化。
- 验证：T-03 线程单元测试与 Electron 端到端验证通过；合并后 `npm run check`、`npm run smoke` 通过；协调者额外做了纯 Node 功能验证（mock 回复、设置读写、消息持久化、空输入校验）。
- 说明：codex/m1-chat 上曾存在另一套 T-03 实现（e95c411：deepseek.js、json-store.js/message-store.js/settings-store.js），与 842f18a 无法共存；按用户决定集成时删除 e95c411 那套，保留 m1-llm 实现（见 ADR-007）。
- 真实调用：设置环境变量 DEEPSEEK_API_KEY（或 settings.set 写入 apiKey）；模型默认取契约 deepseek-v4-flash，可用 DEEPSEEK_MODEL 覆盖。
