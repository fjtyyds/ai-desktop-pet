# T-49 小部件紧凑化与情绪中性化

- 状态：已完成（2026-08-11，已验收合并）
- 分支：codex/m4-widget-mood
- 工作树：无独立 worktree（说明：本卡由用户与 T-48 线程直接交流产生，worker 在 main 工作区直接开发并新建分支；协调者复核后已恢复 main 工作区，分支/提交完整保留）
- 目标：
  1. 天气/番茄钟小部件紧凑化：减少 padding/字号/按钮尺寸，番茄钟标题与倒计时合并一行，降低面板占位；
  2. 情绪中性化：默认 60→52（“平静”区间）、反馈幅度 ±8→±12（上限 24）、无交互回归基线后小幅“呼吸”波动（确定性伪随机），避免“永远愉悦”与长期定格。
- 来源：用户 2026-08-11 与 T-48 线程直接交流改进方案后，由该线程实施并提交 d0fbc70（超出 T-48 任务卡边界，另立本卡记录）。
- 涉及文件：
  - src/llm/mood.js（默认情绪/反馈幅度/呼吸波动）
  - src/renderer/index.html（番茄钟小部件行结构）
  - src/renderer/chat.css（天气/番茄钟紧凑样式）
  - docs/tasks/T-49.md（本卡）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/main/**、src/storage/**、src/llm/provider.js、src/shared/**（除本卡外）、scripts/orchestrator/**、docs/**（除本卡外）
- 验收标准：
  1. `npm run check`、`npm run smoke` 通过（含既有 T-48 元素 id 断言）
  2. 天气/番茄钟小部件高度显著下降，专注/喝水/待办等其他 widget 不受影响；既有元素 id（weather-*/pomodoro-*）保留
  3. mood：默认 valence=52、反馈默认 12/上限 24、呼吸波动确定性（伪随机种子）、不跳极端、无交互回归后再呼吸
  4. 未触碰禁止文件；无外部副作用

## 完成记录（2026-08-11，worker 已填写）

- 分支 codex/m4-widget-mood，提交 d0fbc70（基线 main eff85b0）。
- 天气小部件：padding 8px→5px、图标 20px→15px、温度 18px→15px、折叠/刷新按钮 22/24px→20px、meta 字号 11→10px；实测折叠高度约 31px。
- 番茄钟小部件：标题与倒计时合并为 `.pomodoro-head` 一行，时间 20→16px、按钮 12px/6px→11px/4px、卡片 padding 与间距收窄；实测高度约 61px。
- mood.js：DEFAULT_VALENCE 60→52（平静区间 46-55）；DEFAULT_FEEDBACK_AMOUNT 8→12、MAX_FEEDBACK_AMOUNT 20→24；新增 regressed 标志与 AMBIENT_DRIFT（周期 4 分钟、valence ±5、intensity ±0.05，Math.sin 确定性伪随机）；无交互回归到基线后进入小幅呼吸，不会跳到极端。
- 验证：worker `npm run check`/`npm run smoke` 全绿；离屏 DOM 实测天气 31px、番茄钟 61px。

## 验收合并记录（2026-08-11，协调者）

- 复核：改动 3 个文件（mood.js/index.html/chat.css）均在本卡范围；未触碰禁止文件；元素 id 保留；52 确在“平静”区间；check/smoke 复跑全绿。
- 流程问题（已记录）：本卡为 T-48 线程越界产出，且在主工作区直接开发/切换分支（T-44/T-45 同类问题复现）；协调者已将主工作区恢复 main，分支与提交完整保留；已重申“任务必须独立 worktree、main 工作区保持 main”纪律。
- 合并：建卡/ADR 提交（ad69004）后 rebase 分支到 main 并 fast-forward 合并（rebase 后提交 026c1c5）；合并后 main check/smoke 全绿；无独立 worktree 需清理。
