# T-05 记忆存储与历史接口

- 状态：已完成
- 分支：codex/m2-memory
- 工作树：E:\codex\AI桌宠-m2-memory
- 目标：消息模型升级（sessionId/timestamp）、长期记忆存储（memories.json）、petAPI.history.get
- 涉及文件：
  - src/storage/memory-store.js（新建：消息读写 + 长期记忆增删查，兼容旧 messages.json）
  - src/main/ipc.js（新增 history:get handler）
  - src/main/preload.js（暴露 petAPI.history.get）
- 只读文件：src/shared/contracts.js（协调者已冻结 M2 契约：ChatMessage.sessionId/timestamp、MemoryItem、history.get、DEFAULT_SHORT_TERM_WINDOW、MAX_MEMORIES_IN_CONTEXT）
- 禁止触碰：src/renderer/**、src/llm/**、package.json
- 依赖：契约冻结提交（已就绪）
- 验收标准：
  1. `npm run check` 通过；`npm run smoke` 通过
  2. history.get 返回持久化消息（含旧数据兼容，缺省 sessionId='default'）
  3. memories 可新增/查询/删除，持久化到 memories.json
  4. 纯 Node 单元验证：写入→重读一致
- 完成定义：全部验收通过并提交到 codex/m2-memory

## 设计依据

- ADR-010：M2 记忆模型——短期窗口 + 长期事实记忆（本地 JSON）

## 完成记录（2026-08-09）

- 实现：新建 `src/storage/memory-store.js`，提供 `createMemoryStore(baseDir)`：
  - 消息读写：`readMessages({ sessionId? })` / `appendMessages(items, { sessionId? })`，兼容旧 `messages.json`，缺省 `sessionId='default'`；新消息自动补 `sessionId` 与 `timestamp`。
  - 长期记忆：`listMemories` / `getMemories`（关键词 + 最近使用时间排序，默认上限 `MAX_MEMORIES_IN_CONTEXT`）/ `addMemory` / `touchMemory` / `deleteMemory`，落盘 `memories.json`。
- 接线：`src/main/ipc.js` 新增 `history:get` handler，返回归一化消息历史；`src/main/preload.js` 暴露 `petAPI.history.get()`。
- 验证：纯 Node 单元验证 10 项全部通过（写入→重读一致、旧数据缺省 sessionId、memories 增删查与跨实例持久化、非法/空数据过滤）；`node --check` 通过；`npm run check` 通过；`npm run smoke` 通过（GPU state invalid 为 offscreen 噪音，退出码 0）。
- 未改动：`contracts.js`、`src/renderer/**`、`src/llm/**`、`package.json`；`PLAN.md` / `docs/STATUS.md` 按协调者要求未动。
