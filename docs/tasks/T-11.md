# T-11 崩溃上报与本地日志

- 状态：未开始
- 分支：codex/m3-crash
- 工作树：E:\codex\AI桌宠-m3-crash
- 目标：崩溃与未捕获异常本地留痕（crashReporter + logs/app.log）
- 涉及文件：
  - src/main/crash.js（新建：crashReporter 初始化、本地 dump 目录、未捕获异常/unhandledRejection 写日志）
  - src/main/main.js（引入 crash.js）
  - .gitignore（忽略 logs/ 与 crash dump 目录）
- 只读文件：scripts/check.js（协调者维护）、src/renderer/**、src/storage/**、src/llm/**、package.json
- 禁止触碰：src/renderer/**、src/storage/**、src/llm/**、package.json
- 依赖：ADR-017
- 验收标准：
  1. `npm run check`、`npm run smoke` 通过
  2. 模拟未捕获异常/拒绝的 Promise：logs/app.log 有记录，应用不静默退出
  3. crashReporter 本地 dump 目录可配置（默认 userData/crashes），异常时生成 dump 或至少不报错
- 完成定义：全部验收通过并提交到 codex/m3-crash

## 设计依据

- ADR-017（崩溃上报与本地日志）
