# T-25 工具栏改造（导出 + 最小化）

- 状态：已完成（2026-08-10 合并 main @ 01e35a8）（契约已冻结：petAPI.window.minimize / IPC window:minimize，见 ADR-026）
- 分支：codex/m3x-toolbar
- 工作树：E:\codex\AI桌宠-m3x-toolbar
- 目标：聊天工具栏新增最小化按钮；导出对话从设置页移入工具栏
- 涉及文件：
  - src/main/main.js（window:minimize IPC → BrowserWindow.minimize）
  - src/main/preload.js（petAPI.window.minimize）
  - src/renderer/index.html（工具栏按钮、移除设置页导出区块）
  - src/renderer/chat.js（导出菜单/按钮逻辑、最小化调用、移除设置页导出绑定）
  - src/renderer/chat.css（工具栏样式）
  - src/shared/locales/zh-CN.json、en.json（按钮文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md（协调者按 ADR 冻结新契约前保持只读；冻结后按新签名实现）
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：已完成（ADR-026 冻结契约，contracts.js/API.md 已同步）
- 验收标准：
  1. 工具栏有“最小化”按钮，点击后窗口最小化到任务栏（区别于 ✕ 隐藏到托盘）
  2. 工具栏可导出 Markdown/JSON，保存对话框可用，导出内容正确
  3. 设置页不再显示导出区块
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。
- 协调者合并：01e35a8（2026-08-10；check/smoke 通过）

## 完成记录（2026-08-10）

- 实现：
  - `src/main/main.js`：`WINDOW_CHANNELS.minimize = 'window:minimize'`，新增 `handleWindowMinimize`（`BrowserWindow.fromWebContents(event.sender).minimize()`）并注册；窗口 `minimize` 事件时停止贴边轮询，避免对最小化窗口做位置动画。
  - `src/main/preload.js`：新增 `windowMinimize` 通道与 `petAPI.window.minimize()`。
  - `src/renderer/index.html`：工具栏新增“最小化”按钮（—）与导出下拉菜单（⇩，含 Markdown/JSON 两项与状态提示）；移除设置页“导出对话”区块。
  - `src/renderer/chat.js`：新增 `minimizeWindow`（调用 `petAPI.window.minimize`）、导出菜单开合/外部点击/Escape 关闭逻辑；导出逻辑复用原 `exportConversation`（元素由设置页迁移到工具栏）。
  - `src/renderer/chat.css`：新增工具栏导出菜单样式（绝对定位下拉、菜单项、状态提示）。
  - `src/shared/locales/zh-CN.json`、`en.json`：新增 `window.minimize` / `window.minimizeTitle`。
  - `scripts/check.js`：新增 T-25 同步断言（通道/按钮/菜单/设置页无导出区块/双语文案）。
- 自检结果：`npm.cmd run check` 通过；`npm.cmd run smoke` 通过（真实用户权限）；临时 Electron 端到端验证通过（petAPI.window.minimize 可调用且窗口实际最小化、工具栏 DOM 齐全、设置页导出按钮数量为 0）。
- 目检：未运行 `npm run dev` 可视化窗口目检，标记为“待人工目检”（无头验证已覆盖 DOM/API 与最小化行为）。
- 边界：仅改动任务卡“涉及文件”清单内文件；未触碰 package.json/package-lock.json/contracts.js/API.md/PLAN.md/STATUS.md/AGENTS.md/DECISIONS.md/scripts/orchestrator/**；依赖安装仅发生在工作树 node_modules（未入库）。
