# T-52 MSIX 打包实施与商店版自动更新守卫

- 状态：已完成（待协调者验收，2026-08-11）
- 分支：codex/m4-msix-impl
- 工作树：E:\codex\AI桌宠-m4-msix-impl（协调者已创建，基线 main dd29cc5）
- 目标：按 T-47 验证报告（docs/store/msix-validation.md）落地 MSIX 打包能力，与 NSIS/GitHub 直装并存；商店版禁用 electron-updater（MSIX 自动更新走商店），本地完成构建验证。真实商店注册/提审需用户账号与预算，不在本卡范围。
- 前置（协调者已完成）：electron-builder 升级至 27.0.0-alpha.6（package.json/package-lock.json，ADR-040）；worker 不得再改 package.json/package-lock.json。
- 涉及文件：
  - electron-builder.yml
  - src/main/updater.js
  - src/shared/locales/zh-CN.json、en.json
  - assets/appx/（新增 StoreLogo 50×50、Square150x150Logo、Square44x44Logo、Wide310x150Logo，png）
  - scripts/check.js（如需新增 T-52 断言）
  - docs/tasks/T-52.md（本卡）；docs/store/msix-validation.md（允许补充实施记录，不改既有结论）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、src/shared/locales/index.js、scripts/orchestrator/**、docs/**（除本卡与 msix-validation.md 补充记录外）
- MSIX 配置要求（依据 T-47 报告 §2）：
  - electron-builder.yml win.target 追加 msix（保留 nsis），配置 msix 区块：identityName/publisher/publisherDisplayName（占位值需明确标注待商店账号确定后替换）、languages zh-CN/en-US、版本四段（setBuildNumber 或显式 0.1.0.0）、runFullTrust 默认。
  - assets/appx/ 四个必选 logo（尺寸按微软规格）；如无法生成正式品牌图，可用占位图并标注“待品牌素材替换”。
  - updater.js：initUpdater 前加 `if (process.windowsStore) return;`（Electron 43 支持）；托盘“检查更新”在商店版禁用或提示走商店；双语文案同步。
- 验收标准：
  1. `npm run check` 全绿；`npm run smoke` 全绿
  2. `npm run dist -- --publish never` 产出 .msix/.msixupload 与 NSIS 安装包；NSIS 产物与 latest.yml/app-update.yml 引用一致（更新链路不受影响）
  3. process.windowsStore 守卫代码存在且逻辑正确；商店版不会发起 GitHub 更新请求
  4. 工具链问题（Windows Kits/makeappx）按 T-47 §2.3 处理并记录；不安装系统级组件（可下载用户态缓存/指向已有 SDK）
  5. 未触碰禁止文件；无外部副作用；不合并 main、不 push、不创建 Release
- 完成定义：实现并自检（check/smoke/dist 本地验证），更新本卡状态为“已完成（待协调者验收）”，提交分支；回报（分支/提交/改动文件清单/验证结果/MSIX 与 NSIS 产物路径大小/守卫验证/工具链处理/遗留风险）。
- 备注：worktree 已由协调者建好且 node_modules 已装；不要新建其他 worktree，不要改 main 工作区；MSIX 为 beta 能力，产物仅供本地验证与后续提审准备。

## 完成记录（worker 填写）

- 状态：已完成（待协调者验收，2026-08-11）
- 分支：codex/m4-msix-impl（worktree E:\codex\AI桌宠-m4-msix-impl，基线 main 324c1f7）
- 提交号：919d837（T-52: MSIX 打包实施与商店版更新守卫；随后一个纯文档提交回填本行）
- 实现摘要：
  - electron-builder.yml：win.target 追加 msix（x64，保留 nsis）；新增 msix 块（artifactName `ai-desktop-pet-${version}-${arch}.${ext}`、identityName `AIDesktopPet.AiDesktopPet`、publisher `CN=Placeholder-Publisher`、publisherDisplayName “AI 桌宠项目组”、languages zh-CN/en-US、setBuildNumber true → 版本 0.1.0.0、createMsixupload true；runFullTrust 由默认 capabilities 自动注入）。占位值已在配置注释与 README 标注“待商店账号确定后替换”。
  - assets/appx/：新增 4 个必选占位 logo（StoreLogo 50×50、Square150x150Logo 150×150、Square44x44Logo 44×44、Wide310x150Logo 310×150，PNG）+ README.md 标注待品牌素材替换。
  - src/main/updater.js：initUpdater 最前面加 `if (process.windowsStore)` 守卫，短路返回 no-op API（不注册 autoUpdater 事件、checkForUpdates 不发起任何请求）。
  - src/main/tray.js：商店版点击“检查更新”弹原生 dialog 提示走 Microsoft Store。
  - src/shared/locales/zh-CN.json、en.json：补充 updater.storeUpdateTitle/storeUpdateBody。
  - scripts/check.js：新增 T-52 断言（MSIX 配置/守卫前置/托盘提示/双语文案/logo 尺寸/dist 产物）；同步修复 T-38 latest.yml 解析以兼容 electron-builder 27 的 “- url:” 列表格式（T-47 §3.8 预判项）。
- 改动文件：
  - electron-builder.yml
  - src/main/updater.js
  - src/main/tray.js
  - src/shared/locales/zh-CN.json
  - src/shared/locales/en.json
  - assets/appx/StoreLogo.png、Square44x44Logo.png、Square150x150Logo.png、Wide310x150Logo.png、README.md
  - scripts/check.js
  - docs/tasks/T-52.md（本卡）；docs/store/msix-validation.md（追加 §8 实施记录）
- 验证结果：
  - `npm run check` 全绿（T-38 latest.yml 引用与实际产物一致、T-39 app-update.yml 指向 github/fjtyyds/ai-desktop-pet、T-52 全部断言通过）
  - `npm run smoke` 全绿
  - `npm run dist -- --publish never` 成功（约 78 秒）
  - MSIX 产物：`dist\ai-desktop-pet-0.1.0-x64.msix`（150,468,253 B ≈ 143.5 MiB，未签名=商店代签预期）、`dist\ai-desktop-pet-0.1.0-x64.msixupload`（150,267,768 B，商店提审用）
  - NSIS 产物：`dist\ai-desktop-pet-0.1.0-Setup.exe`（108,736,270 B ≈ 103.7 MiB）+ `.blockmap` + `latest.yml`（引用一致）；`win-unpacked/resources/app-update.yml` 仍为 github/fjtyyds/ai-desktop-pet，更新链路未受影响
  - 包内 AppxManifest.xml 核验：Identity `AIDesktopPet.AiDesktopPet`、Publisher `CN=Placeholder-Publisher`、Version `0.1.0.0`、languages zh-CN/en-US、runFullTrust 能力、4 个 logo 均在包内 assets/
  - 守卫行为（stub 模拟）：商店版 initUpdater 不注册事件（0 次）、checkForUpdates 不调用 autoUpdater（0 次）、返回 null；普通版注册 5 个事件且 checkForUpdates 正常调用 autoUpdater
  - 托盘商店版行为：点击“检查更新”进入 storeUpdateTitle/storeUpdateBody 原生提示（代码路径 + check 断言验证，真机待后续）
  - 工具链：本机无 Windows Kits 时 electron-builder 自动下载 `windows-kits-bundle-10_0_26100_0.zip` 到用户态缓存 `C:\Users\HP\AppData\Local\electron-builder\Cache\win-codesign@1.3.0\`（8,286,066 B）；未安装系统级组件、未使用 ELECTRON_BUILDER_WINDOWS_KITS_PATH
- 遗留风险：
  - MSIX 为 electron-builder 27 alpha 的 beta 能力，升级 27 稳定版后需重测配置与产物（ADR-040 已记录可回退 26.15.3）
  - identityName/publisher/publisherDisplayName 为占位值，需在 Partner Center 注册商店账号、保留名称后回填；.msixupload 由商店代签
  - 未做真机 MSIX 安装/侧载验证（userData 隔离、托盘、通知、卸载行为，T-47 §3 检查项），未配置自签证书
  - 本地产物未签名（NSIS 与 MSIX 均 NotSigned），正式发布签名属 T-46 范围
  - 商店版与直装版 userData 隔离、MSIX 卸载清除全部数据等产品行为需发布前处理（T-47 风险 4/5）

## 验收合并记录（协调者填写）

- 复核：
- 验证：
- 遗留：
