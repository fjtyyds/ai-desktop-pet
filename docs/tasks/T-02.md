# T-02 聊天面板 UI

- 状态：已完成
- 分支：codex/m1-chat
- 工作树：E:\codex\AI桌宠-m1-chat
- 目标：聊天面板 UI（消息列表、输入框、发送、设置页骨架）；通过 `window.petAPI.chat.send` 发消息（接口可能尚未实现，需优雅降级提示）
- 涉及文件：
  - src/renderer/index.html
  - src/renderer/styles.css
  - src/renderer/renderer.js
  - src/renderer/chat.js（新建，聊天逻辑）
  - src/renderer/chat.css（新建，聊天样式）
- 只读文件：src/shared/contracts.js（接口契约，不允许修改）
- 禁止触碰：src/main/**、src/llm/**、src/storage/**、package.json
- 验收标准：
  1. `npm run check` 通过
  2. `npm run smoke` 通过
  3. 手动 `npm run dev`：消息列表可显示；输入可发送；petAPI 未就绪时显示“AI 服务未就绪”而不是崩溃
- 完成定义：全部验收通过并提交到 codex/m1-chat

## 完成记录（2026-08-09）

- 实现：提交 ca9e9c7（codex/m1-chat）——消息列表、输入框/发送、设置页骨架（宠物名 localStorage）；通过 `window.petAPI.chat.send` 发消息，petAPI 缺失时显示“AI 服务未就绪”并优雅降级。
- 验证：`npm run check` 通过；`npm run smoke` 通过；无头 UI 测试通过（无 petAPI 降级不崩溃；集成 T-03 后 mock 回复正常）。
- 说明：dev 模式存在 CSP 警告（M0 起），不属于 T-02 验收范围，打包前由协调者安排补上。
- 任务卡状态由协调者按并行规则更新。
