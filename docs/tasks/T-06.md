# T-06 人格与情绪引擎

- 状态：未开始
- 分支：codex/m2-persona
- 工作树：E:\codex\AI桌宠-m2-persona
- 目标：人格定义与 system prompt 生成、情绪状态机（纯逻辑，无 IPC/存储）
- 涉及文件：
  - src/llm/persona.js（新建：默认人格 + buildSystemPrompt({ settings, mood })）
  - src/llm/mood.js（新建：valence 0-100 状态机、交互更新、情绪描述词）
  - src/llm/index.js（如需导出新模块）
- 只读文件：src/shared/contracts.js（Persona、MoodState 类型已冻结）
- 禁止触碰：src/renderer/**、src/storage/**、src/main/**、package.json
- 依赖：契约冻结提交（已就绪）
- 验收标准：
  1. `npm run check` 通过；`npm run smoke` 通过
  2. 纯 Node 单元验证：默认人格/自定义人格都能生成含 mood 的 system prompt
  3. mood 随交互更新且有限幅（0-100），长时间无交互缓慢回归默认
- 完成定义：全部验收通过并提交到 codex/m2-persona

## 设计依据

- ADR-011：M2 人格与情绪
