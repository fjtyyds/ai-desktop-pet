# T-04 M1 人工验收与收尾

- 状态：已完成
- 分支：main（协调者任务，不新建 worktree）
- 目标：人工目检 M1 验收标准第 3 条与 SPEC 完整流程；补 dev 模式 CSP；可选真实 DeepSeek 调用
- 涉及文件：
  - src/renderer/index.html（CSP）
  - src/renderer/chat.js、src/renderer/chat.css（关闭按钮）
  - src/shared/contracts.js、src/main/preload.js、src/main/ipc.js（window.hide 契约与 IPC）
  - src/main/tray.js、src/main/main.js（如目检发现托盘问题）
  - docs/DECISIONS.md、docs/STATUS.md、PLAN.md
- 验收标准：
  1. `npm run dev` 目检：托盘图标可见、右键菜单“显示/隐藏/退出”可用、左键切换、关窗驻留托盘、单实例锁生效
  2. 聊天 UI：消息列表、输入发送（mock 回复）、petAPI 未就绪降级提示
  3. 设置页：宠物名保存后重读；重启后对话记录仍在（SPEC 完整流程）
  4. dev 控制台无 CSP 警告
  5. 可选：设置 `DEEPSEEK_API_KEY` 后真实调用一次
- 完成定义：目检通过，结果写入 `docs/STATUS.md`、`PLAN.md` 并提交

## 执行记录（2026-08-09）

- 诊断：内嵌托盘图标 PNG（64x64 RGBA）解码与缩放正常，Electron 实测 Tray 创建成功；用户此前未看到图标疑为启动环境或系统托盘折叠导致。
- 修复/改进：渲染页补充 CSP（ADR-008）。
- 目检发现：无边框窗口缺关闭按钮；补齐 ✕ 按钮后发现 Electron 43 的 renderer `window.close()` 不触发主进程可拦截的 close 事件，导致应用退出。改为 petAPI `window.hide` IPC 隐藏窗口，并增加 `window-all-closed` 兜底（ADR-009）。

## 完成记录（2026-08-09）

- 人工目检通过：托盘图标可见、右键菜单「显示/隐藏/退出」与左键切换正常、聊天 mock 收发正常、设置保存正常、单实例正常；关闭按钮点击后隐藏到托盘且应用不退出（修复后复测通过）。
- CSP：dev 控制台无 CSP 警告（端到端验证无违例日志）。
- 校验：`npm run check`、`npm run smoke` 通过。
- 真实调用（可选项）：使用 `DEEPSEEK_API_KEY` 环境变量验证 `deepseek-v4-flash`（契约默认模型）与 `deepseek-chat` 均成功返回中文回复，无需 `DEEPSEEK_MODEL` 覆盖。
