# T-42 匿名遥测与留存漏斗

- 状态：已完成（已验收合并，2026-08-11，ADR-032 上线方案）
- 分支：codex/m4-telemetry（建议）
- 工作树：E:\codex\AI桌宠-m4-telemetry（建议）
- 目标：opt-in 匿名遥测：安装/启动/对话/付费转化/留存事件，本地缓存+批量上报（自建轻量端点或第三方，端点可配）；隐私合规（不采集消息内容、密钥、文件路径；设置页可一键关闭）。
- 涉及文件：
  - src/main/telemetry.js（新增：事件缓冲、批量上报、关闭开关、脱敏）
  - src/main/main.js、ipc.js、preload.js（初始化与开关接线）
  - src/renderer/chat.js、index.html（设置页遥测开关与隐私说明）
  - src/shared/locales/zh-CN.json、en.json（遥测文案）
  - scripts/check.js（断言：默认关闭或首次弹窗询问、脱敏规则、开关持久化）
  - docs/tasks/T-42.md（本卡完成记录）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md、docs/reports/2026-08-11-商业化上线方案.md
- 禁止触碰：package.json、package-lock.json（如需依赖先报协调者）、electron-builder.yml、.github/**、scripts/orchestrator/**、docs/**（除本卡外）；禁止采集敏感数据
- 依赖：无（T-40 合并后补付费事件联动）
- 验收标准：
  1. 首次启动 opt-in 弹窗；关闭后不再采集且本地数据可清除
  2. 事件含安装/启动/对话/付费/留存，字段脱敏（无消息正文、无 apiKey、无绝对路径）
  3. 断网缓存、重连批量上报；上报失败不影响应用
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push；回报协调者。

## T-42 完成记录（2026-08-11）

### 分支 / 工作树

- 分支：codex/m4-telemetry（未合并 main、未 push）
- 工作树：E:\codex\AI桌宠-m4-telemetry（自 main e58f4f3 创建）

### 实现摘要

- 新增 src/main/telemetry.js：opt-in 匿名事件（app_install/app_start/session_end/chat_sent/chat_reply/license_state_change/pomodoro_complete/weather_refresh）、本地队列持久化（queue.json）、断网缓存、批量上报（fetch POST，端点环境变量 AI_PET_TELEMETRY_ENDPOINT 配置，默认空=不发送）、一键清除（队列+device.json）、字段/事件双白名单脱敏、deviceId 随机 UUID 且与许可证解耦。模块不依赖 Electron，可在纯 Node 中单测。
- store.js：telemetryEnabled 白名单字段，默认 false。
- main.js：app ready 时初始化（开关实时读设置，未开启不采集）；app_start/session_end 事件；online 事件触发补发；before-quit 尽力补发。
- ipc.js：telemetry:get-status / set-enabled / flush 三通道；chat_sent/chat_reply/pomodoro_complete/weather_refresh 埋点（仅聚合字段）。
- preload.js：petAPI.telemetry.* 暴露。
- index.html/chat.js/chat.css：首次引导 step3 opt-in 复选框（默认关闭）+ 设置页开关、隐私说明、“关闭并清除遥测数据”按钮。
- locales zh-CN/en：遥测文案。
- check.js：静态断言（模块/端点默认空/无内置真实端点/事件清单/IPC/preload/渲染层接线）+ 运行时断言（默认关闭、开关持久化、脱敏、断网缓存、本地端点批量上报、一键清除、deviceId UUID/许可证解耦）。

### 验证结果

1. `npm run check`：通过（含 T-42 全部新增断言，exit 0）。
2. `npm run smoke`：通过（渲染加载/历史恢复/番茄钟信号/表单端到端，exit 0）。
3. Electron 端到端临时脚本（本地 127.0.0.1 端点，不入库）：
   - 默认关闭、设置页与引导复选框默认不勾选；未开启前无 deviceId；
   - 开启后 chat_sent/chat_reply 入队且队列不含消息正文；
   - telemetry:flush 本地端点批量上报成功，batch.deviceId 为 UUID，不含正文/apiKey；
   - 一键关闭并清除：enabled=false、deviceId=null、queuedCount=0、device.json 已删除；
   - 关闭后再发消息：队列保持为空（不再采集）。
4. 边界：仅改动任务卡允许文件；未触碰 package.json/package-lock/electron-builder.yml/.github/scripts-orchestrator/contracts/API.md/PLAN/STATUS/DECISIONS/AGENTS.md；未向任何真实外部端点发送数据（测试仅用 127.0.0.1 临时端点）。

### 遗留风险 / 说明

- license_state_change 事件已实现并导出 trackLicenseState（状态/档位聚合字段），等待 T-40 许可证模块合并后接线；当前无许可证代码可挂钩。
- 上报端点为环境变量配置（无 UI 端点设置），符合“端点可配置、默认空=不发送”边界。
- 断网/失败时事件保留在本地队列，下次启动或网络恢复（app online）时补发；退出时的 flush 为尽力而为，失败保留队列。
- 首次启动弹窗以现有三步引导的第 3 步 opt-in 复选框形式呈现（引导本身即首次启动弹窗），默认关闭；老用户（onboardingDone=true）不弹，可在设置页开关。

## 协调者验收（2026-08-11）

- fd0b3a3 fast-forward 合并入 main；main check/smoke 通过；worktree 已清理，分支保留。
