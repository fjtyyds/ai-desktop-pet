# T-33 TTS 专属语音包（按人格）

- 状态：已完成（待协调者验收）（2026-08-10，ADR-027）
- 分支：codex/m3x-tts-voice（建议）
- 工作树：E:\codex\AI桌宠-m3x-tts-voice（建议）
- 目标：T-23 系统 TTS 朗读语气僵硬；为 6 套预设人格各配置专属语音包（语音/音调/语速），朗读时按当前生效人格应用，可关闭回退系统默认
- 涉及文件：
  - src/renderer/chat.js（TTS 语音选择与参数映射、按 personaTemplate 应用）
  - src/renderer/index.html（设置页语音包开关/选择入口）
  - src/renderer/chat.css（设置项样式）
  - src/storage/store.js（如需新增 ttsVoicePack 等字段，先经协调者确认白名单）
  - src/shared/locales/zh-CN.json、en.json（语音包文案）
  - scripts/check.js（同步断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：T-24~T-31 已合并；与 T-32 可并行评估或先后实施
- 验收标准：
  1. 6 套人格各有一套默认语音参数（voice/pitch/rate），切换人格后朗读风格随之变化
  2. 设置页可关闭专属语音包（回退系统默认 TTS）或选择语音包
  3. T-23 朗读按钮行为正常，朗读生硬感显著改善
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。

## 实现记录（2026-08-10）

- 分支/工作树：codex/m3x-tts-voice / E:\codex\AI桌宠-m3x-tts-voice（基线 main 0eae4d0）
- 实现：
  - store.js 仅登记协调者预确认的两个 settings 字段：`ttsVoicePackEnabled`（默认 true）、`ttsVoicePackId`（默认 ''，≤40 字符，空=自动跟随当前 personaTemplate），并补读取清洗与写入白名单。
  - chat.js 新增 `TTS_VOICE_PACKS`：6 套人格（warm/sage/playful/gentle/cool/curious）各配 voice 偏好（语言前缀+系统语音名称关键词）与 pitch/rate；朗读时按当前生效人格自动应用，机器无匹配语音时回退界面语言默认语音但仍应用 pitch/rate。
  - 设置页（index.html + chat.js + chat.css + 双语 locale）：专属语音包开关（关闭禁用下拉并回退系统默认 TTS）+ 语音包下拉（自动跟随人格/6 套固定选择），随语言即时重绘。
  - scripts/check.js 增加 T-33 同步断言：语音包表 6 套齐全、设置页控件、双语文案、store 默认值/白名单范围（仅两个 tts* 字段）/读写清洗。
- 验证：`npm run check` 全部通过；`npm run smoke` 通过（真实用户权限）；`npm run dev` 启动无报错（窗口正常出现，日志无错误；视觉效果待协调者/用户人工目检）；临时无头 UI 校验 10 项全部通过（开关默认开启、下拉 7 选项、关闭禁用/开启恢复、显式保存、英文重绘）。
- 边界外触碰：无（package.json/package-lock/contracts/API.md/PLAN/STATUS/AGENTS/DECISIONS/orchestrator 均未改动）。
- 待确认：无。
