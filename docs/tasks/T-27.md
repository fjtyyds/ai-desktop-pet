# T-27 番茄钟结束通知重复弹窗修复

- 状态：未开始
- 分支：codex/m3x-pomodoro-fix
- 工作树：E:\codex\AI桌宠-m3x-pomodoro-fix
- 目标：修复番茄钟结束后系统通知反复弹出的问题（疑似消费/清零竞态），保证同一完成信号只弹一次
- 涉及文件：
  - src/main/main.js（consumePomodoroNotificationRequest 幂等消费、清零后再弹、同信号去重）
  - src/storage/store.js（pomodoroNotifyAt 信号读写与清理语义）
  - src/renderer/chat.js（finishPomodoro 写入信号、避免 saveSettings 回写陈旧信号）
  - src/main/ipc.js（如涉及设置写入路径）
  - scripts/check.js、scripts/smoke.js（补重复通知回归断言）
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md
- 禁止触碰：package.json、package-lock.json、src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、AGENTS.md、docs/DECISIONS.md、scripts/orchestrator/**
- 依赖：无
- 验收标准：
  1. 倒计时结束只弹一次系统通知，后续状态轮询不再重复
  2. 主进程消费与清零幂等；渲染层不会在普通设置保存时回写已消费信号
  3. 手动停止/重置后不产生通知
  4. `npm run check` 通过；`npm run smoke` 通过或说明原因
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main；完成后先回报协调者。
