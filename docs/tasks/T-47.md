# T-47 MSIX 打包与商店沙箱技术验证

- 状态：已完成（待协调者验收）（2026-08-11，T-46 遗留事项）
- 分支：codex/m4-msix（建议）
- 工作树：E:\codex\AI桌宠-m4-msix（建议）
- 目标：在提审前验证 MSIX 打包与 Microsoft Store 沙箱兼容性：①electron-builder 对 MSIX 的支持现状与工具链要求；②沙箱环境下 userData 写入、自动更新链路、单实例锁、托盘、通知、safeStorage 的兼容风险；③给出推荐方案（MSIX vs MSI/EXE 保留）与工作量评估。只出报告与本地实验，不提交商店、不注册账号、不购买任何服务。
- 涉及文件：
  - docs/store/msix-validation.md（新增：验证报告，含结论/风险清单/推荐方案/工作量）
  - docs/tasks/T-47.md（本卡状态与完成记录）
  - 允许本地实验：临时目录（mktemp）、只读 npm 包文档、electron-builder 官方文档；如需在仓库内新增脚本或改动 package.json/electron-builder.yml/src/**，必须先回报协调者，不得擅自修改。
- 只读文件：src/shared/contracts.js、docs/API.md、PLAN.md、docs/STATUS.md、docs/DECISIONS.md、AGENTS.md、docs/reports/2026-08-11-商业化上线方案.md
- 禁止触碰：package.json、package-lock.json、electron-builder.yml、.github/**、src/**、scripts/orchestrator/**、docs/**（除本卡与 docs/store 外）
- 依赖：T-46（商店素材与提审待办）
- 验收标准：
  1. 报告包含 electron-builder MSIX 支持结论（支持/不支持/需要额外工具与配置）
  2. 报告包含沙箱兼容风险清单（userData、更新、单实例、托盘、通知、safeStorage/DPAPI、安装/卸载行为）及每项验证结论或风险等级
  3. 报告包含推荐方案（MSIX 优先 vs MSI/EXE 并存）与预计工作量
  4. 未触碰禁止文件；无外部账号/资金操作；`npm run check` 通过
- 完成定义：实现并自检，更新本卡状态为“已完成（待协调者验收）”，提交分支；不合并 main、不 push；回报协调者。

## 完成记录（2026-08-11）

- 分支：codex/m4-msix；worktree：E:\codex\AI桌宠-m4-msix（基线 main 935dbb3）
- 交付物：
  - docs/store/msix-validation.md：MSIX 打包与商店沙箱技术验证报告。核心结论：
    1. electron-builder 有 MSIX 目标但为 beta：仅存在于 27.0.0-alpha 线（app-builder-lib@27.0.0-alpha.6 实测含 MsixTarget/MsixOptions/.msixupload）；项目锁定 26.15.3 及 26.15.x 全线无 msix（仅 appx 目标，产物 .appx）。要使用需协调者批准依赖升级。
    2. 工具链：Windows 10+ 构建机；需现代 Windows Kits（makeappx/makepri/signtool，SDK 10.0.26100），electron-builder 可自动下载（winCodeSign ≥ 1.0.0），拒绝 legacy 2.6.0；微软官方另推 winapp CLI / Forge maker-msix（需本机 Windows SDK）。
    3. 沙箱逐项：userData 重定向到 %LOCALAPPDATA%\Packages\<family>\LocalCache\Roaming\<name>（可写，与直装版隔离，代码无需改动）；自动更新高风险（electron-updater 不支持 MSIX，需 process.windowsStore 守卫）；单实例锁低风险；托盘/通知中风险需真机验证；safeStorage/DPAPI 可用但数据隔离+卸载清数据；MSIX 卸载清除全部应用数据，需导出引导。
    4. 推荐：MSIX 上架 + NSIS/GitHub 直装并存；预计 3.5–5.5 人日（含依赖升级、更新守卫、真机冒烟、CI、提审物料回填）。
  - 本卡状态与完成记录。
- 验证：
  - `npm run check` 通过（worktree 首次 npm install 后全绿；含 T-37~T-45 现有断言；dist 不存在故 T-38/T-39 产物断言跳过，属预期）。
  - 本地只读实验：npm pack app-builder-lib@26.15.7 / 27.0.0-alpha.6 至 %TEMP% 解包比对（26.15.7 无 msix，alpha 有），electron-updater 6.8.9 源码 0 处 MSIX 处理。
  - 未执行真实 MSIX 构建/安装（需先升级依赖并改 electron-builder.yml，属边界外，已列后续步骤）。
- 边界合规：只新增 docs/store/msix-validation.md 与更新本卡；未触碰 package.json、package-lock.json、electron-builder.yml、.github/**、src/**、scripts/orchestrator/** 及其余 docs/**；未注册商店账号、未购买、未提交商店、未 push、未合并 main。
- 遗留风险：
  1. electron-builder MSIX 目标为 beta（27.0.0-alpha），配置面仍可能变化，升级后必须重测。
  2. 商店版若直接跑现有 updater.js 会误用 electron-updater，需实施 process.windowsStore 守卫（后续任务）。
  3. MSIX 托盘/通知/单实例/safeStorage 需真实安装包逐项验证；失败可回退 NSIS 上架评估。
  4. 商店版与直装版 userData 隔离、卸载清数据，需产品侧引导与文档。
  5. MSIX 提审还需 Partner Center 名称保留后回填 identityName/publisher、隐私 URL 与 T-46 素材对接。
